---
description: Автоисправление lint ошибок для всего проекта
---

# Lint Fix

Автоматическое исправление ошибок форматирования и линтинга.

## Outline

1. Форматирование Python (black) для backend и ml
2. Автоисправление линтинга (ruff --fix) для backend и ml
3. Автоисправление ESLint для frontend

## Implementation

```bash
#!/usr/bin/env bash

set -e

echo "🔧 Автоисправление lint ошибок..."
echo ""

ERRORS=0

# Backend Python
if [ -d "backend/src" ]; then
    echo "📝 Backend: форматирование (black)..."
    cd backend

    if black --line-length 100 .; then
        echo "✅ Backend: black применён"
    else
        echo "❌ Backend: ошибка black"
        ERRORS=$((ERRORS + 1))
    fi

    echo ""
    echo "📝 Backend: автоисправление (ruff)..."

    if ruff check --fix .; then
        echo "✅ Backend: ruff --fix применён"
    else
        echo "⚠️ Backend: ruff нашёл неисправимые ошибки"
    fi

    cd ..
    echo ""
fi

# ML Python
if [ -d "ml/src" ]; then
    echo "📝 ML: форматирование (black)..."
    cd ml

    if black --line-length 100 .; then
        echo "✅ ML: black применён"
    else
        echo "❌ ML: ошибка black"
        ERRORS=$((ERRORS + 1))
    fi

    echo ""
    echo "📝 ML: автоисправление (ruff)..."

    if ruff check --fix .; then
        echo "✅ ML: ruff --fix применён"
    else
        echo "⚠️ ML: ruff нашёл неисправимые ошибки"
    fi

    cd ..
    echo ""
fi

# Frontend TypeScript
if [ -d "frontend" ] && [ -f "frontend/package.json" ]; then
    echo "📝 Frontend: автоисправление (eslint)..."
    cd frontend

    if npm run lint -- --fix; then
        echo "✅ Frontend: eslint --fix применён"
    else
        echo "⚠️ Frontend: eslint нашёл неисправимые ошибки"
    fi

    cd ..
    echo ""
fi

echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

if [ $ERRORS -eq 0 ]; then
    echo "✅ Автоисправление завершено"
    echo ""
    echo "Проверьте изменения: git diff"
else
    echo "⚠️ Автоисправление завершено с предупреждениями"
    echo ""
    echo "Некоторые ошибки требуют ручного исправления"
fi
```

## What Gets Fixed

**Black (Python):**
- Отступы (4 пробела)
- Длина строк (макс 100 символов)
- Пробелы вокруг операторов
- Кавычки (двойные)

**Ruff (Python):**
- Неиспользуемые импорты
- Неиспользуемые переменные
- Порядок импортов
- Некоторые PEP8 нарушения

**ESLint (TypeScript):**
- Пробелы и отступы
- Точки с запятой
- Кавычки (одинарные)
- Trailing commas

## Notes

- Некоторые ошибки требуют ручного исправления
- После исправления проверьте: `git diff`
- Перед коммитом запустите: `/test.all`