---
description: Запуск pytest тестов для ml сервиса
---

# Test ML

Запуск тестов ml с поддержкой фильтрации.

## Arguments

`$ARGUMENTS` - паттерн для фильтрации тестов (опционально)

## Outline

1. Перейти в директорию ml/
2. Запустить pytest с заданными параметрами
3. Показать результаты

## Implementation

```bash
#!/usr/bin/env bash

set -e

if [ ! -d "ml/tests" ]; then
    echo "❌ Директория ml/tests/ не найдена"
    exit 1
fi

cd ml

echo "🧪 Запуск ml тестов..."
echo ""

# Проверка аргументов для фильтрации
if [ -n "$ARGUMENTS" ]; then
    echo "📌 Фильтр: $ARGUMENTS"
    python -m pytest -xvs --tb=short -k "$ARGUMENTS"
else
    python -m pytest -xvs --tb=short
fi
```

## Usage Examples

```bash
# Все тесты
/test.ml

# Только тесты с "course" в названии
/test.ml course

# Только тесты с "generation"
/test.ml generation
```

## Pytest Flags

- `-x` - остановка на первой ошибке
- `-v` - verbose режим
- `-s` - показывать print() вывод
- `--tb=short` - короткий traceback
- `-k PATTERN` - фильтрация по имени теста

## Notes

- Тесты запускаются в виртуальном окружении ml
- ML тесты могут быть медленными (генерация курсов через LLM)
- Для пропуска slow тестов: `/test.ml -m "not slow"`