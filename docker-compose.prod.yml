# Production overrides.
# Использование: docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d
# docker-compose.override.yml при этом НЕ применяется автоматически.
# Базовый docker-compose.yml уже не содержит dev volumes и dev command —
# этот файл нужен только для prod-специфичных параметров (если потребуются).
