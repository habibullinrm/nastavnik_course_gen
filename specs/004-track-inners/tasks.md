# Tasks: Просмотр внутреннего содержимого трека

**Input**: Design documents from `/specs/004-track-inners/`
**Prerequisites**: plan.md ✅, spec.md ✅, research.md ✅, data-model.md ✅, contracts/api.md ✅

**Organization**: Задачи сгруппированы по User Story для независимой реализации и проверки.

## Format: `[ID] [P?] [Story] Description`

- **[P]**: Можно запускать параллельно (разные файлы, нет зависимостей)
- **[Story]**: К какой User Story относится задача

---

## Phase 1: Setup (Структура)

**Purpose**: Создать новые директории и файлы типов, не трогая существующий код.

- [X] T001 Создать директорию `frontend/src/components/Track/` для новых компонентов трека
- [X] T002 Создать файл TypeScript-типов для разделов track_data в `frontend/src/types/track.ts` (CompetencySetData, KSAMatrixData, LearningUnitsData, HierarchyData, LessonBlueprintsData, ScheduleData, ValidationData и все вложенные интерфейсы из data-model.md)

---

## Phase 2: Foundational (Блокирующие Prerequisites)

**Purpose**: Переиспользуемые компоненты и базовая инфраструктура, необходимые всем User Story.

**⚠️ КРИТИЧНО**: Ни одна User Story не может быть реализована до завершения этой фазы.

- [X] T003 Создать переиспользуемый компонент-заглушку EmptySection в `frontend/src/components/Track/EmptySection.tsx` — принимает `message: string` и опциональный `step: string`, отображает информативное сообщение об отсутствии данных

**Checkpoint**: Foundational завершена — можно начинать User Story фазы

---

## Phase 3: User Story 1 — Просмотр полного содержимого трека (Priority: P1) 🎯 MVP

**Goal**: Исследователь видит все выходы B2, B3, B6, B7 в читаемом виде на странице трека.

**Independent Test**: Открыть любой completed-трек — компетенции, ЗУН-матрица, PBL-сценарии и расписание отображаются без raw JSON.

### Implementation

- [X] T004 [P] [US1] Реализовать компонент TrackMetadata (замена заглушки) в `frontend/src/components/TrackMetadata/TrackMetadata.tsx` — отображает статус каждого шага B1–B8 (завершён/не выполнен/ошибка) на основе `generation_metadata.steps_log` и `track.status`; показывает общее время генерации, версию алгоритма
- [X] T005 [P] [US1] Реализовать компонент CompetencyList в `frontend/src/components/Track/CompetencyList.tsx` — принимает `data?: CompetencySetData`, если null показывает EmptySection; отображает карточки компетенций (title + description + level), выделяет integral_competency
- [X] T006 [P] [US1] Реализовать компонент KSAMatrix в `frontend/src/components/Track/KSAMatrix.tsx` — три плоских секции «Знания / Умения / Навыки» (нет related_competency_ids в схеме); null data → EmptySection
- [X] T007 [P] [US1] Реализовать компонент LessonBlueprints в `frontend/src/components/Track/LessonBlueprints.tsx` — принимает `data?: LessonBlueprintsData`; отображает список Blueprint-карточек с `problem_formulation.problem_statement`, `knowledge_infusions[]`, `practice_tasks[]`; null → EmptySection «PBL-сценарии не сгенерированы»
- [X] T008 [P] [US1] Реализовать компонент WeeklySchedule (замена заглушки) в `frontend/src/components/WeeklySchedule/WeeklySchedule.tsx` — принимает `data?: ScheduleData`; список недель с checkpoint-выделением (amber цвет); итого: total_weeks, final_assessment.type; null → EmptySection
- [X] T009 [US1] Обновить страницу трека `frontend/src/app/tracks/[id]/page.tsx` — подключить все компоненты T004–T008 через 7-вкладочный TrackTabs; передать данные из `track.track_data`; EmptySection для каждого раздела при отсутствии данных; убран raw JSON

**Checkpoint**: US1 полностью функциональна — страница трека показывает B2/B3/B6/B7 без raw JSON

---

## Phase 4: User Story 2 — Навигация между разделами (Priority: P2)

**Goal**: Исследователь переключается между вкладками мгновенно, без перезагрузки страницы.

**Independent Test**: Открыть трек → последовательно переключить все 6 вкладок за < 3 сек без ошибок.

**Depends on**: US1 (T004–T009) — компоненты разделов должны существовать

### Implementation

- [X] T010 [US2] Создать компонент TrackTabs в `frontend/src/components/Track/TrackTabs.tsx` — горизонтальная панель вкладок (sticky при скролле): Метаданные / Компетенции / ЗУН-матрица / Учебные единицы / PBL-сценарии / Расписание / Поля профиля; активная вкладка подсвечивается; 7 вкладок
- [X] T011 [US2] Интегрировать TrackTabs в страницу трека `frontend/src/app/tracks/[id]/page.tsx` — реализовано как часть T009; tab-навигация sticky

**Checkpoint**: Полная навигация работает — все 6 вкладок переключаются мгновенно

---

## Phase 5: User Story 3 — Дерево учебных единиц с деталями (Priority: P2)

**Goal**: Исследователь видит B4/B5 дерево с типом и длительностью каждого узла; в расписании выделены checkpoint-недели.

**Independent Test**: Открыть вкладку «Учебные единицы» — каждый узел имеет тип (theory/practice/automation) и длительность в минутах; в «Расписании» — недели с checkpoints визуально выделены.

**Depends on**: T008 (WeeklySchedule существует), T011 (TabPanel «Учебные единицы» подключён)

### Implementation

- [X] T012 [P] [US3] Реализовать компонент LearningTree (замена TreeView-заглушки) в `frontend/src/components/TreeView/TreeView.tsx` — принимает `units?: LearningUnitsData` и `hierarchy?: HierarchyData`; дерево: Уровень → Кластер → Юниты (theory/practice/automation с цветными бейджами и estimated_minutes); null → EmptySection
- [X] T013 [P] [US3] Checkpoint-выделение реализовано в T008 (WeeklySchedule): amber-цвет для недель с checkpoint, бейдж «Контрольная точка», раскрывающийся блок с assessment_tasks

**Checkpoint**: US3 готова — дерево отображает типы и длительность, schedule выделяет checkpoint-недели

---

## Phase 6: User Story 4 — Быстрый переход к треку со страницы профиля (Priority: P3)

**Goal**: Исследователь открывает профиль и видит ссылку на последний трек.

**Independent Test**: Открыть `/profiles` → нажать на профиль → страница профиля показывает ссылку «Открыть последний трек» (или кнопку генерации если треков нет).

### Implementation

- [X] T014 [P] [US4] Добавить схему LastTrackResponse в `backend/src/schemas/student_profile.py` — поля: `track_id: UUID`, `status: str`, `created_at: datetime`
- [X] T015 [US4] Добавить endpoint `GET /api/profiles/{profile_id}/last-track` в `backend/src/api/profiles.py` — async def, запрашивает PersonalizedTrack для profile_id с ORDER BY created_at DESC LIMIT 1; возвращает LastTrackResponse или 404
- [X] T016 [P] [US4] Создать страницу детального просмотра профиля в `frontend/src/app/profiles/[id]/page.tsx` — загружает профиль и последний трек параллельно; ссылка «Открыть последний трек →» или кнопка «Генерировать трек»
- [X] T017 [US4] Обновить страницу списка профилей `frontend/src/app/profiles/page.tsx` — названия профилей стали ссылками на `/profiles/{id}`

**Checkpoint**: US4 готова — профиль → ссылка на трек работает

---

## Phase 7: Polish & Cross-Cutting Concerns

**Purpose**: Проверка edge cases, финальная валидация.

- [X] T018 [P] Null/undefined обработка реализована в каждом компоненте: все принимают optional data?, EmptySection при отсутствии
- [X] T019 [P] Обновить Assumptions в `specs/004-track-inners/spec.md` — убрать устаревшее «существующие компоненты сохраняются»
- [ ] T020 Провести ручную проверку по сценариям из `specs/004-track-inners/quickstart.md` — completed трек, failed трек, генерирующий трек, профиль без треков

---

## Dependencies & Execution Order

### Phase Dependencies

- **Setup (Phase 1)**: Нет зависимостей — начинать сразу
- **Foundational (Phase 2)**: Зависит от Phase 1 — блокирует US1
- **US1 (Phase 3)**: Зависит от Phase 2 — MVP; блокирует US2
- **US2 (Phase 4)**: Зависит от US1 (компоненты должны существовать)
- **US3 (Phase 5)**: Зависит от Phase 2; частично зависит от US1 (T008 WeeklySchedule)
- **US4 (Phase 6)**: Независима от US1–US3 (отдельные файлы: backend + profile page)
- **Polish (Phase 7)**: Зависит от завершения всех User Story

### User Story Dependencies

- **US1 (P1)**: После Phase 2 — независима, MVP
- **US2 (P2)**: После US1 — зависит от компонентов US1
- **US3 (P2)**: После Phase 2 — независима от US1/US2 (отдельные компоненты)
- **US4 (P3)**: После Phase 2 — полностью независима

### Parallel Opportunities

Внутри Phase 3 (US1) задачи T004–T008 полностью параллельны (разные файлы):
```
T004 TrackMetadata.tsx
T005 CompetencyList.tsx    ← все 5 одновременно
T006 KSAMatrix.tsx
T007 LessonBlueprints.tsx
T008 WeeklySchedule.tsx
```

Внутри Phase 5 (US3) T012 и T013 параллельны:
```
T012 TreeView.tsx (LearningTree)
T013 WeeklySchedule.tsx (checkpoint highlight)  ← одновременно
```

Внутри Phase 6 (US4) T014 и T016 параллельны:
```
T014 backend schema
T016 frontend profile page  ← одновременно
```

---

## Parallel Example: User Story 1

```bash
# Все 5 компонентов US1 можно реализовывать одновременно:
Task T004: "Реализовать TrackMetadata в frontend/src/components/TrackMetadata/TrackMetadata.tsx"
Task T005: "Реализовать CompetencyList в frontend/src/components/Track/CompetencyList.tsx"
Task T006: "Реализовать KSAMatrix в frontend/src/components/Track/KSAMatrix.tsx"
Task T007: "Реализовать LessonBlueprints в frontend/src/components/Track/LessonBlueprints.tsx"
Task T008: "Реализовать WeeklySchedule в frontend/src/components/WeeklySchedule/WeeklySchedule.tsx"
# Затем T009 — интеграция в page.tsx
```

---

## Implementation Strategy

### MVP First (User Story 1 Only)

1. Завершить Phase 1: Setup (T001–T002)
2. Завершить Phase 2: Foundational (T003)
3. Завершить Phase 3: US1 (T004–T009)
4. **СТОП и ВАЛИДАЦИЯ**: Открыть completed-трек — все 4 раздела (B2/B3/B6/B7) читаемы
5. Демо готово

### Incremental Delivery

1. Phase 1+2 → Foundation ready
2. US1 (T004–T009) → Контент виден → **MVP**
3. US2 (T010–T011) → Удобная навигация добавлена
4. US3 (T012–T013) → Дерево + checkpoint-недели
5. US4 (T014–T017) → Навигация с профиля
6. Polish (T018–T020) → Готово к релизу

---

## Notes

- `[P]` = разные файлы, нет зависимостей между собой
- `[Story]` = трейсабельность к User Story из spec.md
- Коммит после каждой задачи или логической группы
- TrackMetadata и WeeklySchedule — **замена заглушек** в существующих директориях компонентов
- TreeView — **замена заглушки** в `frontend/src/components/TreeView/TreeView.tsx`
- CompetencyList, KSAMatrix, LessonBlueprints, EmptySection, TrackTabs — **новые** компоненты в `frontend/src/components/Track/`
- Типы в `frontend/src/types/track.ts` — детализированные схемы из data-model.md
