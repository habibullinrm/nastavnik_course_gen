# ФАЗА B: ПРОЕКТИРОВАНИЕ ПЕРСОНАЛИЗИРОВАННОГО УЧЕБНОГО ТРЕКА

## Общие принципы фазы B

### Цель
На основе `StudentProfile` (выход фазы A) спроектировать полный персонализированный учебный трек: от компетенций до понедельного расписания с учебными единицами.

### Исполнитель
Все шаги фазы B выполняет **LLM** — без участия пользователя. Пользователь видит только готовый трек на выходе.

### Вход
`StudentProfile` — полный структурированный профиль студента из фазы A.

### Выход
`PersonalizedTrack` — готовая структура трека, содержащая компетенции, матрицу ЗУН, учебные единицы, иерархию уровней, проблемные формулировки для каждой темы, понедельное расписание.

### Методологическая основа
- **Модель 4C/ID** — каркас проектирования (учебные задачи → поддерживающая информация → процедурная информация → частичная практика).
- **Проблемно-ориентированный подход** — каждая тема начинается с практической проблемы (ПП), а не с теории.
- **Принцип ЗУН** — компетенции декомпозируются на Знания → Умения → Навыки.

### Принцип работы LLM
На каждом шаге LLM получает:
1. **Инструкцию шага** — что нужно сделать.
2. **Входные данные** — конкретные поля из `StudentProfile` или выходы предыдущих шагов.
3. **Формат выхода** — TypeScript-интерфейс ожидаемого результата.
4. **Критерии качества** — как проверить, что результат корректен.
5. **Примеры** — образцы входа и выхода для калибровки.

### Связь с фазами A и C
```
Фаза A (сбор данных)          Фаза B (проектирование)              Фаза C (проведение)
─────────────────────         ─────────────────────────            ──────────────────────
StudentProfile ──────────────► B1 → B2 → B3 → B4 → B5 → B6 → B7 → B8
                                                                    │
                                                        PersonalizedTrack ──────► FSM урока
```

---

## ОБЗОР ШАГОВ

| Шаг | Название | Суть | Зависит от |
|---|---|---|---|
| **B1** | Валидация и обогащение профиля | Проверка полноты, заполнение пробелов, определение `effective_level` | `StudentProfile` |
| **B2** | Формулирование компетенций | Перевод задач и outcomes в формат «Способен [действие] в [контекст]» | B1 |
| **B3** | Декомпозиция компетенций в матрицу ЗУН | Каждая компетенция → Знания + Умения + Навыки | B2 |
| **B4** | Проектирование учебных единиц | ЗУН → конкретные темы, задания, упражнения с учётом предпочтений | B3 |
| **B5** | Построение иерархии и уровней сложности | Распределение учебных единиц по уровням, определение зависимостей | B4 |
| **B6** | Проектирование проблемных формулировок | Для каждой темы: ПП + сценарий урока для фазы C | B5 |
| **B7** | Сборка трека в расписание | Понедельное расписание с учётом временного бюджета | B6 |
| **B8** | Валидация трека | Проверка полноты, непротиворечивости, достижимости KPI | B7 |

### Граф зависимостей шагов фазы B

```
StudentProfile (из фазы A)
    │
    ▼
   B1: Валидация и обогащение профиля
    │
    ▼
   B2: Формулирование компетенций
    │
    ▼
   B3: Декомпозиция в матрицу ЗУН
    │
    ▼
   B4: Проектирование учебных единиц
    │
    ▼
   B5: Иерархия и уровни сложности
    │
    ▼
   B6: Проблемные формулировки (ПП для фазы C)
    │
    ▼
   B7: Сборка трека в расписание
    │
    ▼
   B8: Валидация трека
    │
    ▼
PersonalizedTrack (вход для фазы C)
```

---

## ШАГ B1: ВАЛИДАЦИЯ И ОБОГАЩЕНИЕ ПРОФИЛЯ

### Название
`validate_and_enrich_profile`

### Суть
Проверить `StudentProfile` на полноту и непротиворечивость. Заполнить поля со значимостью 🟡 `IMPORTANT`, для которых использованы значения по умолчанию. Определить `effective_level` — реальный стартовый уровень на основе пересечения самооценки (`experience_level`) и диагностики (`diagnostic_result`).

### Зачем нужен
Профиль из фазы A может содержать:
- Противоречия (самооценка ≠ диагностика).
- Неполные данные (поля со значениями по умолчанию).
- Данные, сгенерированные системой (`*_source == "system_generated"`) — их нужно верифицировать на согласованность.

### Входные данные
| Поле | Источник | Описание |
|---|---|---|
| Все поля `StudentProfile` | Фаза A | Полный профиль |

### Логика шага

#### 1. Проверка CRITICAL-полей
```
ДЛЯ КАЖДОГО поля со значимостью CRITICAL:
    ЕСЛИ поле пустое или null:
        ОШИБКА: "Невозможно построить трек — отсутствует [field_name]"
        → Вернуть управление в фазу A для дополнительного сбора
```

#### 2. Определение effective_level
Матрица пересечения `experience_level` × `diagnostic_result`:

| experience_level \ diagnostic_result | `no_knowledge` | `misconceptions` | `partial` | `solid_base` |
|---|---|---|---|---|
| `zero` | **absolute_beginner** | **absolute_beginner** | beginner | beginner |
| `beginner` | absolute_beginner | **beginner** | beginner | intermediate_ready |
| `intermediate` | beginner ⚠️ | beginner ⚠️ | **intermediate** | intermediate |
| `advanced` | beginner ⚠️ | intermediate ⚠️ | intermediate ⚠️ | **advanced** |

⚠️ = расхождение самооценки и диагностики. Система доверяет диагностике (`diagnostic_result`), но фиксирует расхождение в `identified_risks`.

```
ЕСЛИ experience_level == "intermediate" И diagnostic_result == "no_knowledge":
    effective_level = "beginner"
    identified_risks.push("Значительное расхождение самооценки и диагностики. 
        Пользователь оценивает себя как intermediate, но диагностика показала no_knowledge.")
```

#### 3. Обогащение system_generated данных
```
ЕСЛИ tasks_source == "system_generated":
    Проверить: покрывают ли target_tasks все desired_outcomes?
    ЕСЛИ НЕТ: дополнить target_tasks задачами, покрывающими пропущенные outcomes.

ЕСЛИ subtasks_source == "system_generated":
    Проверить: достаточна ли декомпозиция для уровня effective_level?
    ЕСЛИ effective_level == "absolute_beginner":
        Добавить более мелкие подзадачи (более детальная декомпозиция).

ЕСЛИ concepts_source == "auto_generated":
    Проверить: все ли ключевые термины темы покрыты?
    Дополнить пропущенные.
```

#### 4. Расчёт временных параметров
```
available_total_hours = weekly_hours * estimated_weeks (предварительная оценка)
ЕСЛИ has_deadline:
    max_weeks = (deadline_date - today) / 7
    ЕСЛИ max_weeks < 2:
        identified_risks.push("Критически мало времени. Трек будет сжатым.")
```

### Выходные данные

| Поле | Тип | Описание |
|---|---|---|
| `validated_profile` | `ValidatedStudentProfile` | Профиль с заполненными пробелами |
| `effective_level` | enum | Реальный стартовый уровень |
| `enrichment_log` | string[] | Что было дополнено/исправлено |
| `validation_warnings` | string[] | Предупреждения о расхождениях |
| `estimated_weeks_range` | [number, number] | Предварительная оценка длительности [min, max] |

### Критерии качества
- Все CRITICAL-поля заполнены.
- `effective_level` определён.
- Нет логических противоречий между полями (пример: `goal_type == "applied"`, но `desired_outcomes` содержат только теоретические результаты).
- `target_tasks` покрывают все `desired_outcomes`.

---

## ШАГ B2: ФОРМУЛИРОВАНИЕ КОМПЕТЕНЦИЙ

### Название
`formulate_competencies`

### Суть
Перевести задачи студента (`target_tasks`, `desired_outcomes`, `peak_task`) в профессиональные формулировки компетенций формата: **«Способен [действие] в условиях [контекст]»**.

### Зачем нужен
Компетенция — это «свёрнутый ответ» на вопрос студента «Что я буду уметь после обучения?». Она является мостом между целями студента и конкретным содержанием обучения. Без компетенций невозможно системно декомпозировать содержание на ЗУН.

### Входные данные
| Поле | Источник | Описание |
|---|---|---|
| `target_tasks` | B1 (validated) | Массив целевых задач |
| `task_hierarchy` | B1 | Задачи, упорядоченные от простой к сложной |
| `peak_task_id` | B1 | Вершина мастерства |
| `subtasks` | B1 | Подзадачи вершины мастерства |
| `desired_outcomes` | B1 | Целевые результаты обучения |
| `primary_context` | B1 | Контекст применения |
| `context_type` | B1 | Тип контекста (academic / professional / personal / general) |
| `topic` | B1 | Тема обучения |
| `subject_area` | B1 | Предметная область |
| `effective_level` | B1 | Реальный стартовый уровень |

### Логика шага

#### 1. Определение интегральной компетенции (из вершины мастерства)
```
peak_task = target_tasks.find(t => t.id == peak_task_id)

LLM-инструкция:
    "На основе задачи-вершины мастерства: '{peak_task.description}'
     и контекста применения: '{primary_context}'
     сформулируй ИНТЕГРАЛЬНУЮ компетенцию в формате:
     'Способен [глагол действия] [объект] с использованием [инструмент/метод] 
      в условиях [контекст из primary_context]'
     
     Требования:
     - Глагол должен быть из таксономии Блума уровня «применение» и выше.
     - Контекст берётся из primary_context.
     - Компетенция должна быть достижима за время обучения."
```

#### 2. Формулирование составных компетенций (из иерархии задач)
```
ДЛЯ КАЖДОГО уровня сложности в task_hierarchy:
    Сгруппировать задачи по тематической близости.
    ДЛЯ КАЖДОЙ группы:
        Сформулировать компетенцию, покрывающую задачи группы.
        Связать с интегральной компетенцией (отношение «часть — целое»).
```

#### 3. Валидация полноты
```
ДЛЯ КАЖДОГО desired_outcome:
    Проверить: существует ли хотя бы одна компетенция, покрывающая этот outcome.
    ЕСЛИ НЕТ: сформулировать дополнительную компетенцию.
```

#### 4. Учёт уровня студента
```
ЕСЛИ effective_level IN ["absolute_beginner", "beginner"]:
    Добавить пропедевтическую компетенцию уровня «понимание»:
    "Способен объяснять базовые понятия [topic] и их взаимосвязи"
    (эта компетенция гарантирует, что фундамент будет заложен)
```

### Выходные данные

| Поле | Тип | Описание |
|---|---|---|
| `competencies` | `Competency[]` | Массив сформулированных компетенций |
| `integral_competency_id` | string | ID интегральной компетенции (из вершины мастерства) |
| `competency_task_map` | `Map<competency_id, task_id[]>` | Связь «компетенция → задачи» |
| `competency_outcome_map` | `Map<competency_id, outcome_index[]>` | Связь «компетенция → desired_outcomes» |

### Критерии качества
- Каждая задача из `target_tasks` покрыта хотя бы одной компетенцией.
- Каждый `desired_outcome` покрыт хотя бы одной компетенцией.
- Интегральная компетенция соответствует `peak_task`.
- Формулировки содержат конкретный глагол действия + контекст.
- Количество компетенций: от 2 до 7 (при большем числе — объединить тематически близкие).

---

## ШАГ B3: ДЕКОМПОЗИЦИЯ КОМПЕТЕНЦИЙ В МАТРИЦУ ЗУН

### Название
`decompose_to_ksa_matrix`

### Суть
Разложить каждую компетенцию на три категории индикаторов:
- **Знания (З)** — что нужно знать (факты, правила, модели).
- **Умения (У)** — что нужно уметь делать осознанно (процедуры, алгоритмы).
- **Навыки (Н)** — что должно быть доведено до автоматизма.

Формула декомпозиции: **«Чтобы быть способным [компетенция], учащийся должен знать...; уметь...; владеть...»**

### Зачем нужен
Матрица ЗУН — это **конструктор содержания курса**. Каждый элемент ЗУН превращается в конкретную учебную единицу: Знание → тема теории, Умение → практическое задание, Навык → упражнение на автоматизацию.

### Входные данные
| Поле | Источник | Описание |
|---|---|---|
| `competencies` | B2 | Массив компетенций |
| `subtasks` | B1 | Подзадачи вершины мастерства |
| `key_barriers` | B1 | Концептуальные барьеры из профиля |
| `confusing_concepts` | B1 | Термины, вызывающие путаницу |
| `approach_gaps` | B1 | Выявленные пробелы в текущем подходе |
| `mastery_signals` | B1 | Субъективные маркеры освоения навыка |
| `success_criteria` | B1 | Критерии успеха |
| `already_known_subtasks` | B1 | Подзадачи, которые уже известны |
| `effective_level` | B1 | Стартовый уровень |
| `topic` | B1 | Тема |

### Логика шага

#### 1. Генерация Знаний (З)
Источники для генерации:
- `confusing_concepts` → каждый путающий термин порождает обязательное Знание.
- `key_barriers` с `barrier_type == "conceptual"` → каждый концептуальный барьер порождает Знание.
- `approach_gaps` → каждый пробел в подходе порождает Знание (теоретическое обоснование правильного подхода).
- `subtasks[].required_knowledge` → знания, необходимые для каждой подзадачи.

```
LLM-инструкция:
    "Для компетенции '{competency.statement}':
     Используя следующие источники:
     - Путающие термины: {confusing_concepts}
     - Барьеры: {key_barriers}
     - Пробелы: {approach_gaps}
     - Подзадачи: {subtasks}
     
     Сформулируй список ЗНАНИЙ в формате:
     'Знать [что именно]: [краткое описание содержания]'
     
     Правила:
     - Каждый путающий термин должен быть покрыт хотя бы одним Знанием.
     - Каждый концептуальный барьер должен быть покрыт Знанием.
     - Знания формулируются конкретно: не 'знать логику', а 'знать таблицу истинности для импликации'.
     - Для уровня {effective_level}: если absolute_beginner — включить предпосылочные знания (определения базовых понятий)."
```

#### 2. Генерация Умений (У)
Источники:
- `subtasks` → каждая подзадача порождает Умение.
- `target_tasks` → каждая целевая задача порождает обобщающее Умение.
- `already_known_subtasks` → соответствующие Умения помечаются как `pre_existing = true` (не исключаются, но сокращаются в треке).

```
LLM-инструкция:
    "Для компетенции '{competency.statement}':
     На основе подзадач: {subtasks}
     
     Сформулируй список УМЕНИЙ в формате:
     'Уметь [действие]: [описание контекста применения]'
     
     Правила:
     - Каждая подзадача → минимум одно Умение.
     - Умения описывают осознанное, контролируемое действие.
     - Умения упорядочены от простого к сложному.
     - Если подзадача из already_known_subtasks — пометить соответствующее Умение."
```

#### 3. Генерация Навыков (Н)
Источники:
- `mastery_signals` → субъективные маркеры «что должно работать на автомате».
- `success_criteria` → измеримые критерии, подразумевающие скорость/безошибочность.
- Анализ Умений: какие из них нужно довести до автоматизма для целевого контекста.

```
LLM-инструкция:
    "Для компетенции '{competency.statement}':
     На основе маркеров освоения: {mastery_signals}
     и критериев успеха: {success_criteria}
     
     Определи, какие Умения должны быть доведены до НАВЫКА (автоматизма).
     
     Формулируй навыки в формате:
     'Навык [действия] — [критерий автоматизма: скорость / безошибочность / без внешних подсказок]'
     
     Правила:
     - Не каждое Умение становится Навыком — только те, что критичны для целевого контекста.
     - Навык обязательно содержит измеримый критерий автоматизма.
     - Количество навыков обычно меньше, чем умений."
```

#### 4. Построение матрицы
```
ДЛЯ КАЖДОЙ компетенции:
    Связать: Знания ← обеспечивают → Умения ← формируют → Навыки
    Проверить: каждое Умение имеет хотя бы одно поддерживающее Знание.
    Проверить: каждый Навык построен на конкретном Умении.
```

### Выходные данные

| Поле | Тип | Описание |
|---|---|---|
| `ksa_matrix` | `KSAMatrix` | Полная матрица ЗУН |
| `knowledge_items` | `KnowledgeItem[]` | Все элементы Знаний |
| `skill_items` | `SkillItem[]` | Все элементы Умений |
| `habit_items` | `HabitItem[]` | Все элементы Навыков |
| `ksa_dependency_graph` | `DependencyEdge[]` | Связи: З→У, У→Н, З→З (предпосылки) |

### Критерии качества
- Каждая компетенция имеет минимум 1 Знание, 1 Умение и 1 Навык.
- Каждый `confusing_concept` покрыт соответствующим Знанием.
- Каждая `subtask` покрыта соответствующим Умением.
- Нет «висящих» Умений без поддерживающих Знаний.
- Нет «висящих» Навыков без базовых Умений.
- Общее количество элементов ЗУН адекватно `weekly_hours` × `estimated_weeks` (не слишком много для доступного времени).

---

## ШАГ B4: ПРОЕКТИРОВАНИЕ УЧЕБНЫХ ЕДИНИЦ

### Название
`design_learning_units`

### Суть
Превратить каждый элемент матрицы ЗУН в конкретную учебную единицу (Learning Unit) одного из трёх типов:
- **`theory`** — тема для теории (из Знаний).
- **`practice`** — задание для практики (из Умений).
- **`automation`** — упражнение для автоматизации (из Навыков).

Учебные единицы проектируются с учётом предпочтений студента (формат, обратная связь, время).

### Зачем нужен
Матрица ЗУН — абстрактна. Учебная единица — конкретна: у неё есть тема, формат, длительность, тип обратной связи. Это «кирпичики», из которых собирается трек.

### Входные данные
| Поле | Источник | Описание |
|---|---|---|
| `ksa_matrix` | B3 | Полная матрица ЗУН |
| `knowledge_items` | B3 | Элементы Знаний |
| `skill_items` | B3 | Элементы Умений |
| `habit_items` | B3 | Элементы Навыков |
| `ksa_dependency_graph` | B3 | Связи между элементами |
| `theory_format_preference` | B1 | Предпочтения по формату теории |
| `theory_format_details` | B1 | Детали формата |
| `instruction_format` | B1 | Формат инструкций к практике |
| `feedback_type` | B1 | Тип обратной связи |
| `practice_format` | B1 | Формат практики |
| `daily_practice_minutes` | B1 | Минут в день на микро-тренировки |
| `context_type` | B1 | Тип контекста |
| `primary_context` | B1 | Контекст применения |
| `role` | B1 | Роль студента |
| `effective_level` | B1 | Стартовый уровень |

### Логика шага

#### 1. Генерация учебных единиц типа `theory` (из Знаний)
```
ДЛЯ КАЖДОГО knowledge_item:
    LLM-инструкция:
        "Создай учебную единицу ТЕОРИИ для знания: '{knowledge_item.description}'
         
         Параметры:
         - Формат подачи: {theory_format_preference} ({theory_format_details})
         - Роль студента: {role}
         - Контекст: {primary_context}
         - Стартовый уровень: {effective_level}
         
         Сгенерируй:
         1. title — название темы (краткое, понятное студенту)
         2. description — что студент узнает (1-2 предложения)
         3. content_outline — структура контента (ключевые пункты)
         4. format — конкретный формат подачи (из предпочтений)
         5. estimated_minutes — оценка времени на изучение
         6. prerequisites — ID других учебных единиц, которые нужно пройти до этой
         
         Правила:
         - Если theory_format_preference == 'visual_schemas': формат = визуальная схема/конспект.
         - Если theory_format_preference == 'examples_first': начинать с примера, потом правило.
         - Длительность theory-единицы: 10-30 минут.
         - Для absolute_beginner: добавить больше контекста и пояснений 'зачем это нужно'."
```

#### 2. Генерация учебных единиц типа `practice` (из Умений)
```
ДЛЯ КАЖДОГО skill_item:
    LLM-инструкция:
        "Создай учебную единицу ПРАКТИКИ для умения: '{skill_item.description}'
         
         Параметры:
         - Формат инструкций: {instruction_format}
         - Формат практики: {practice_format}
         - Тип обратной связи: {feedback_type}
         - Контекст задач: {primary_context}
         
         Сгенерируй:
         1. title — название задания
         2. description — что студент будет делать
         3. task_template — шаблон задания (задача + ожидаемый формат ответа)
         4. instruction_type — тип инструкции (checklist / video / simulator / reference_card)
         5. feedback_strategy — как давать обратную связь (из предпочтений)
         6. difficulty_variants — варианты задания разной сложности (min 2: с подсказкой и без)
         7. estimated_minutes — оценка времени
         8. prerequisites — ID предпосылочных учебных единиц
         9. related_knowledge_ids — ID теоретических единиц, которые поддерживают это задание
         
         Правила:
         - Задание должно быть конкретным и проверяемым.
         - Если practice_format содержит 'compare_with_standard': включить эталонное решение.
         - Если instruction_format содержит 'checklist': включить пошаговый алгоритм.
         - Длительность practice-единицы: 15-45 минут.
         - Если skill_item.pre_existing == true: создать только проверочное задание (без обучающей части)."
```

#### 3. Генерация учебных единиц типа `automation` (из Навыков)
```
ДЛЯ КАЖДОГО habit_item:
    LLM-инструкция:
        "Создай учебную единицу АВТОМАТИЗАЦИИ для навыка: '{habit_item.description}'
         
         Параметры:
         - Длительность одной сессии: {daily_practice_minutes} минут
         - Критерий автоматизма: {habit_item.mastery_criterion}
         
         Сгенерируй:
         1. title — название тренажёра
         2. description — что тренируется
         3. exercise_template — шаблон упражнения (множество однотипных задач)
         4. repetition_count — рекомендуемое число повторений за сессию
         5. mastery_threshold — порог освоения (% правильных / скорость / кол-во серий без ошибок)
         6. estimated_sessions — оценка количества сессий до освоения
         7. related_skill_ids — ID умений, на которых построен навык
         
         Правила:
         - Упражнения должны быть короткими (1-3 мин на одно).
         - Мгновенная обратная связь обязательна.
         - Прогрессия сложности: от простых вариаций к сложным.
         - Общее время одной сессии = daily_practice_minutes."
```

#### 4. Связывание учебных единиц по модели 4C/ID
```
ДЛЯ КАЖДОГО умения (skill_item):
    Найти связанные знания (theory-единицы) → Поддерживающая информация (Компонент 2).
    Найти инструкцию к заданию → Процедурная информация (Компонент 3).
    Найти связанный навык (automation-единица) → Частичная практика (Компонент 4).
    
    Объединить в LearningUnitCluster:
        theory_units: [связанные theory-единицы]
        practice_unit: текущая practice-единица
        automation_units: [связанные automation-единицы]
```

### Выходные данные

| Поле | Тип | Описание |
|---|---|---|
| `learning_units` | `LearningUnit[]` | Все учебные единицы |
| `theory_units` | `TheoryUnit[]` | Подмножество: единицы теории |
| `practice_units` | `PracticeUnit[]` | Подмножество: единицы практики |
| `automation_units` | `AutomationUnit[]` | Подмножество: единицы автоматизации |
| `unit_clusters` | `LearningUnitCluster[]` | Группы связанных единиц (по модели 4C/ID) |
| `unit_dependency_graph` | `DependencyEdge[]` | Граф зависимостей между единицами |

### Критерии качества
- Каждый элемент ЗУН покрыт хотя бы одной учебной единицей.
- Каждая practice-единица имеет хотя бы одну связанную theory-единицу.
- Формат каждой единицы соответствует предпочтениям из профиля.
- Суммарная оценочная длительность не превышает `weekly_hours × estimated_weeks × 60` минут (с запасом 20%).
- Для `pre_existing` умений созданы сокращённые единицы.

---

## ШАГ B5: ПОСТРОЕНИЕ ИЕРАРХИИ И УРОВНЕЙ СЛОЖНОСТИ

### Название
`build_hierarchy_and_levels`

### Суть
Распределить учебные единицы по уровням сложности (Базовый → Средний → Продвинутый) и упорядочить внутри каждого уровня с учётом зависимостей.

### Зачем нужен
Учебные единицы из B4 — это неупорядоченная коллекция. Студент не может учить «всё сразу» — нужна последовательность, учитывающая:
1. Логические зависимости (нельзя учить B, не зная A).
2. Диагностированную сложность (субъективно трудные темы — позже и с большей поддержкой).
3. Приоритет целевого контекста (критически важное для KPI — больше времени на отработку).
4. Временные рамки.

### Входные данные
| Поле | Источник | Описание |
|---|---|---|
| `learning_units` | B4 | Все учебные единицы |
| `unit_clusters` | B4 | Кластеры по 4C/ID |
| `unit_dependency_graph` | B4 | Граф зависимостей |
| `task_hierarchy` | B1 | Задачи от простой к сложной |
| `easiest_task_id` | B1 | ID самой простой задачи (точка старта) |
| `peak_task_id` | B1 | ID вершины мастерства |
| `competency_task_map` | B2 | Связь компетенций и задач |
| `key_barriers` | B1 | Барьеры (субъективная сложность) |
| `success_criteria` | B1 | Критерии успеха (приоритет) |
| `weekly_hours` | B1 | Часов в неделю |
| `has_deadline` | B1 | Есть ли дедлайн |
| `deadline_date` | B1 | Дата дедлайна |
| `effective_level` | B1 | Стартовый уровень |
| `already_known_subtasks` | B1 | Уже известные подзадачи |

### Логика шага

#### 1. Топологическая сортировка по зависимостям
```
sorted_units = topological_sort(learning_units, unit_dependency_graph)
// Гарантирует: ни одна единица не стоит раньше своих предпосылок.
```

#### 2. Назначение уровней сложности
```
LLM-инструкция:
    "Распредели учебные единицы по уровням сложности.
    
     Входные данные:
     - Учебные единицы: {sorted_units}
     - Иерархия задач: {task_hierarchy}
     - Стартовый уровень: {effective_level}
     
     Правила назначения уровней:
     
     БАЗОВЫЙ уровень:
     - Единицы, связанные с easiest_task.
     - Пропедевтические Знания (базовые определения, фундаментальные понятия).
     - Простейшие Умения.
     - Для effective_level == 'absolute_beginner': расширить базовый уровень за счёт дополнительных вводных тем.
     
     СРЕДНИЙ уровень:
     - Единицы, связанные с задачами средней сложности.
     - Знания о взаимосвязях между понятиями.
     - Умения, комбинирующие несколько простых действий.
     - Начало формирования навыков.
     
     ПРОДВИНУТЫЙ уровень:
     - Единицы, связанные с peak_task.
     - Интеграционные Знания.
     - Сложные Умения (комбинация множества шагов).
     - Навыки, требующие скорости и безошибочности.
     
     ИНТЕГРАЦИОННЫЙ уровень (финальный):
     - Комплексные задачи, объединяющие все компетенции.
     - Симуляция целевой деятельности (пробный экзамен, итоговый проект).
     - Связан с success_criteria.
     
     Дополнительные правила:
     - Единицы для already_known_subtasks могут быть перенесены на более ранний уровень как проверочные (без обучающей части).
     - Темы из key_barriers: увеличить объём теории и практики на соответствующем уровне.
     - Единицы, критичные для success_criteria: обеспечить больше практических повторений."
```

#### 3. Расчёт временного бюджета по уровням
```
total_minutes = sum(unit.estimated_minutes для всех learning_units)
available_minutes = weekly_hours * 60 * estimated_weeks

ЕСЛИ total_minutes > available_minutes:
    // Сжатие: сократить OPTIONAL-единицы, уменьшить количество повторений
    // Приоритет сохранения: единицы, связанные с success_criteria
    
ЕСЛИ has_deadline:
    max_minutes = weekly_hours * 60 * max_weeks
    ЕСЛИ total_minutes > max_minutes:
        // Критическое сжатие: оставить только CRITICAL-пути
        // Пометить предупреждение

ДЛЯ КАЖДОГО уровня:
    level_minutes = sum(unit.estimated_minutes для единиц уровня)
    level_weeks = ceil(level_minutes / (weekly_hours * 60))
```

#### 4. Формирование последовательности внутри уровня
```
ДЛЯ КАЖДОГО уровня:
    Внутри уровня упорядочить единицы по принципу цикличности 4C/ID:
    
    ДЛЯ КАЖДОГО кластера (unit_cluster) на этом уровне:
        Порядок: Theory → Practice → Automation
        (Студент сначала изучает теорию, затем практикует, затем автоматизирует)
    
    Между кластерами: от более простого к более сложному.
```

### Выходные данные

| Поле | Тип | Описание |
|---|---|---|
| `levels` | `TrackLevel[]` | Массив уровней с единицами |
| `unit_sequence` | `LearningUnit[]` | Глобальная последовательность всех единиц |
| `level_time_budget` | `Map<level_id, { minutes: number, weeks: number }>` | Временной бюджет каждого уровня |
| `total_weeks` | number | Общая длительность трека в неделях |
| `compression_warnings` | string[] | Предупреждения о сжатии (если были) |

### Критерии качества
- Топологический порядок не нарушен (ни одна единица не стоит раньше своих предпосылок).
- Уровни идут строго: Базовый → Средний → Продвинутый → Интеграционный.
- Суммарное время ≤ `weekly_hours × total_weeks × 60` минут.
- Если `has_deadline`: `total_weeks ≤ max_weeks`.
- Каждый `success_criterion` покрыт хотя бы одной единицей на интеграционном уровне.

---

## ШАГ B6: ПРОЕКТИРОВАНИЕ ПРОБЛЕМНЫХ ФОРМУЛИРОВОК

### Название
`design_problem_formulations`

### Суть
Для каждой тематической группы учебных единиц создать **практическую проблему (ПП)** — стартовую точку проблемно-ориентированного урока, который будет проводиться в фазе C.

Каждая ПП включает: формулировку проблемы, ожидаемые гипотезы ученика, ключевую информацию для выдачи, задания на применение, противоречия для углубления, вопросы для рефлексии.

### Зачем нужен
Фаза C работает по модели конечного автомата (FSM) с состояниями: ПП → ГП → КИ → ПМ → ПТ → СЗ → РФ. Но автомату нужны **конкретные данные**: какую проблему ставить, какую теорию давать, какие задания предлагать. Этот шаг готовит весь контент для FSM.

### Входные данные
| Поле | Источник | Описание |
|---|---|---|
| `unit_clusters` | B4 | Кластеры учебных единиц (по 4C/ID) |
| `unit_sequence` | B5 | Глобальная последовательность |
| `levels` | B5 | Уровни сложности |
| `competencies` | B2 | Компетенции |
| `ksa_matrix` | B3 | Матрица ЗУН |
| `role` | B1 | Роль студента |
| `primary_context` | B1 | Контекст применения |
| `context_type` | B1 | Тип контекста |
| `key_barriers` | B1 | Барьеры |
| `approach_gaps` | B1 | Пробелы в подходе |
| `effective_level` | B1 | Стартовый уровень |
| `theory_format_preference` | B1 | Формат теории |
| `feedback_type` | B1 | Тип обратной связи |

### Логика шага

```
ДЛЯ КАЖДОГО кластера (unit_cluster) в unit_sequence:

    LLM-инструкция:
        "Спроектируй проблемно-ориентированный урок для кластера:
         - Теория: {cluster.theory_units}
         - Практика: {cluster.practice_unit}
         - Навык: {cluster.automation_units}
         - Связанная компетенция: {competency}
         - Роль студента: {role}
         - Контекст: {primary_context}
         - Стартовый уровень: {effective_level}
         
         Сгенерируй:
         
         1. ПРАКТИЧЕСКАЯ ПРОБЛЕМА (ПП):
            - Формулировка: реальная ситуация, дилемма или интригующий вопрос из контекста {primary_context}.
            - Форма: один из вариантов — проблемная постановка ('не хватает...'), проблемный вопрос ('что нужно для...?'), конкретная задача ('найди... реши...'), кейс (описание ситуации).
            - Для {context_type}:
              * academic → задачи в стиле экзаменационных.
              * professional → рабочие ситуации.
              * personal → бытовые аналогии.
              * general → универсальные примеры.
            - Для effective_level == 'absolute_beginner': начинать с бытовой аналогии, а не с формальной задачи.
         
         2. ОЖИДАЕМЫЕ ГИПОТЕЗЫ (ГП):
            - 2-4 типичных ответа ученика (правильных, частично правильных, ошибочных).
            - Для каждой гипотезы: какой `diagnostic_result` она отражает.
            - Реакция системы на каждую гипотезу (переход к КИ / ПТ / ПМ — по таблице состояний).
         
         3. КЛЮЧЕВАЯ ИНФОРМАЦИЯ (КИ):
            - Минимально необходимая теория из {cluster.theory_units}.
            - Формат подачи: {theory_format_preference}.
            - Порции: разбить на 1-3 порции, чтобы не перегружать.
            - Каждая порция → за ней следует немедленное ПМ.
         
         4. ЗАДАНИЯ НА ПРИМЕНЕНИЕ (ПМ):
            - 2-3 задания возрастающей сложности из {cluster.practice_unit}.
            - Для каждого задания: эталонный ответ + типичные ошибки + обратная связь.
            - Тип обратной связи: {feedback_type}.
         
         5. ПРОТИВОРЕЧИЕ (ПТ):
            - Усложнённый случай или каверзный вопрос, показывающий ограниченность текущего понимания.
            - Связь с key_barriers: если есть барьер, относящийся к этому кластеру — использовать его как основу ПТ.
            - Связь с approach_gaps: если есть пробел — ПТ должен его обнажить.
         
         6. ЗАДАНИЕ НА СИНТЕЗ (СЗ):
            - Задание, требующее объединить все знания кластера для решения исходной ПП.
            - Критерии полного и неполного ответа.
         
         7. ВОПРОСЫ ДЛЯ РЕФЛЕКСИИ (РФ):
            - 2-3 вопроса типа: 'как ты пришёл к решению?', 'какой шаг был самым трудным?', 'где это можно применить ещё?'
            - Критерии глубокой и поверхностной рефлексии."
```

### Выходные данные

| Поле | Тип | Описание |
|---|---|---|
| `lesson_blueprints` | `LessonBlueprint[]` | Полные сценарии уроков |
| Каждый `LessonBlueprint` содержит: | | |
| — `problem_statement` | ProblemStatement | ПП: формулировка проблемы |
| — `expected_hypotheses` | Hypothesis[] | ГП: ожидаемые гипотезы с реакциями |
| — `key_information_portions` | KIPortion[] | КИ: порции теории |
| — `application_tasks` | ApplicationTask[] | ПМ: задания на применение |
| — `contradiction` | Contradiction | ПТ: противоречие/усложнение |
| — `synthesis_task` | SynthesisTask | СЗ: задание на синтез |
| — `reflection_questions` | ReflectionQuestion[] | РФ: вопросы для рефлексии |
| — `hints` | Hint[] | ПЗ: подсказки для каждого этапа |
| — `fsm_transition_rules` | TransitionRule[] | Правила переходов FSM для этого урока |

### Критерии качества
- Каждый кластер имеет LessonBlueprint.
- Каждая ПП сформулирована в контексте `primary_context`.
- Каждая КИ-порция сопровождается заданием на ПМ.
- Есть хотя бы одно ПТ на урок.
- Правила переходов FSM покрывают все варианты ответов ученика (по таблице состояний из «учебных действий»).
- Для `absolute_beginner`: ПП начинается с бытовой аналогии.

---

## ШАГ B7: СБОРКА ТРЕКА В РАСПИСАНИЕ

### Название
`assemble_track_schedule`

### Суть
Связать все компоненты в **понедельное расписание** с конкретными задачами на каждый день, механизмами поддержки и точками контроля.

### Зачем нужен
Без расписания трек — это набор материалов. Расписание превращает его в пошаговый план, который студент может выполнять день за днём.

### Входные данные
| Поле | Источник | Описание |
|---|---|---|
| `levels` | B5 | Уровни с единицами |
| `unit_sequence` | B5 | Последовательность единиц |
| `lesson_blueprints` | B6 | Сценарии уроков |
| `total_weeks` | B5 | Длительность трека |
| `weekly_hours` | B1 | Часов в неделю |
| `schedule` | B1 | Распределение по дням |
| `daily_practice_minutes` | B1 | Минут на микро-тренировки |
| `practice_windows` | B1 | Временные окна для тренировок |
| `support_tools` | B1 | Механизмы поддержки |
| `needs_reminder` | B1 | Нужны ли напоминания |
| `success_criteria` | B1 | Критерии успеха |
| `learning_format` | B1 | Формат обучения |
| `has_deadline` | B1 | Есть ли дедлайн |

### Логика шага

#### 1. Распределение единиц по неделям
```
ДЛЯ КАЖДОЙ недели (от 1 до total_weeks):
    available_minutes_this_week = weekly_hours * 60
    current_units = []
    
    ПОКА available_minutes_this_week > 0 И есть нераспределённые единицы:
        next_unit = следующая единица из unit_sequence (с учётом зависимостей)
        ЕСЛИ next_unit.estimated_minutes <= available_minutes_this_week:
            current_units.push(next_unit)
            available_minutes_this_week -= next_unit.estimated_minutes
        ИНАЧЕ:
            // Единица не помещается — перенести на следующую неделю
            ПРЕРВАТЬ
    
    week.units = current_units
```

#### 2. Распределение внутри недели (по дням)
```
ДЛЯ КАЖДОЙ недели:
    ДЛЯ КАЖДОГО дня в schedule:
        day_minutes = schedule[day].available_minutes
        
        // Длинные блоки (≥ 60 мин) → теория + практика
        ЕСЛИ day_minutes >= 60:
            Назначить theory + practice единицы
        
        // Короткие блоки (30-59 мин) → практика или облегчённая теория
        ЕСЛИ 30 <= day_minutes < 60:
            Назначить practice единицы
        
        // Микро-блоки (< 30 мин) → автоматизация
        ЕСЛИ day_minutes < 30:
            Назначить automation единицы
    
    // Ежедневные микро-тренировки — поверх основного расписания
    ДЛЯ КАЖДОГО дня:
        ЕСЛИ есть automation-единицы на текущем или предыдущем уровне:
            Добавить daily_practice_minutes автоматизации
```

#### 3. Встраивание точек контроля
```
// Точка контроля в конце каждого уровня
ДЛЯ КАЖДОГО уровня:
    last_week_of_level = последняя неделя уровня
    Добавить checkpoint:
        type: "level_assessment"
        description: "Проверочная работа по уровню [level_name]"
        criteria: задачи из success_criteria, относящиеся к этому уровню
        pass_threshold: 70%
        on_fail: "Повторить ключевые единицы уровня"

// Финальная точка контроля (Интеграционный уровень)
Добавить final_checkpoint:
    type: "final_assessment"
    description: "Итоговая проверка / пробный экзамен / итоговый проект"
    criteria: все success_criteria
    format: имитация целевой деятельности (экзамен, проект, рабочая задача)
```

#### 4. Встраивание механизмов поддержки
```
ЕСЛИ needs_reminder:
    Добавить reminder_schedule: напоминания о микро-тренировках в practice_windows.

ЕСЛИ learning_format == "mentored":
    Добавить mentor_checkpoints: точки связи с ментором (1 раз в неделю или после каждого checkpoint).

ЕСЛИ support_tools содержит "progress_tracker":
    Добавить progress_milestones: промежуточные достижения (% пройденного, навыки освоены).

ДЛЯ КАЖДОГО checkpoint:
    Определить: что делать при непрохождении:
    - Повтор конкретных единиц.
    - Дополнительная практика.
    - Связь с ментором (если mentored).
```

### Выходные данные

| Поле | Тип | Описание |
|---|---|---|
| `weeks` | `TrackWeek[]` | Понедельное расписание |
| Каждая `TrackWeek` содержит: | | |
| — `week_number` | number | Номер недели |
| — `level` | string | Уровень сложности |
| — `theme` | string | Тема недели (человекочитаемая) |
| — `days` | `TrackDay[]` | Расписание по дням |
| — `checkpoint` | `Checkpoint \| null` | Точка контроля (если есть) |
| — `weekly_goals` | string[] | Цели недели |
| Каждый `TrackDay` содержит: | | |
| — `day_of_week` | string | День недели |
| — `main_units` | `LearningUnit[]` | Основные учебные единицы |
| — `automation_units` | `AutomationUnit[]` | Микро-тренировки |
| — `total_minutes` | number | Общее время |
| `checkpoints` | `Checkpoint[]` | Все точки контроля |
| `final_assessment` | `FinalAssessment` | Итоговая оценка |
| `support_plan` | `SupportPlan` | План поддержки |
| `progress_milestones` | `Milestone[]` | Промежуточные достижения |

### Критерии качества
- Суммарное время каждой недели ≤ `weekly_hours × 60` минут (допуск ±10%).
- Суммарное время каждого дня ≤ `schedule[day].available_minutes` (допуск ±15%).
- Ежедневные микро-тренировки присутствуют с недели 1 (по мере появления automation-единиц).
- Точка контроля есть в конце каждого уровня.
- Финальная точка контроля покрывает все `success_criteria`.
- Порядок единиц внутри каждого дня и недели не нарушает зависимости из `unit_dependency_graph`.

---

## ШАГ B8: ВАЛИДАЦИЯ ТРЕКА

### Название
`validate_track`

### Суть
Финальная проверка PersonalizedTrack на полноту, непротиворечивость и достижимость целей. Этот шаг — «контрольный прогон» перед передачей в фазу C.

### Зачем нужен
LLM может допускать ошибки на любом шаге: пропустить зависимость, создать нереалистичное расписание, не покрыть компетенцию. B8 — это защитный слой.

### Входные данные
| Поле | Источник | Описание |
|---|---|---|
| Все выходы шагов B1–B7 | B1–B7 | Полный набор данных трека |
| `StudentProfile` | Фаза A | Оригинальный профиль (для сверки) |

### Проверки

#### 1. Проверка покрытия (Completeness)
```
CHECK_1: Каждый desired_outcome покрыт хотя бы одной компетенцией.
CHECK_2: Каждая компетенция имеет полный набор ЗУН (≥1 З, ≥1 У, ≥1 Н).
CHECK_3: Каждый элемент ЗУН покрыт хотя бы одной учебной единицей.
CHECK_4: Каждая учебная единица включена в расписание.
CHECK_5: Каждый success_criterion покрыт финальной или промежуточной проверкой.
CHECK_6: Каждый confusing_concept покрыт theory-единицей.
CHECK_7: Каждый key_barrier адресован хотя бы одним ПТ (противоречием) в lesson_blueprint.
```

#### 2. Проверка порядка (Ordering)
```
CHECK_8: Для каждой единицы U с prerequisites [P1, P2...]:
    U.week_number >= max(P1.week_number, P2.week_number...)
    Внутри одной недели: U.day_index >= max(Pi.day_index...)
CHECK_9: Уровни идут строго: Базовый → Средний → Продвинутый → Интеграционный.
CHECK_10: Внутри кластера: Theory → Practice → Automation.
```

#### 3. Проверка времени (Feasibility)
```
CHECK_11: ДЛЯ КАЖДОЙ недели: total_minutes <= weekly_hours * 60 * 1.1
CHECK_12: ДЛЯ КАЖДОГО дня: total_minutes <= schedule[day].available_minutes * 1.15
CHECK_13: ЕСЛИ has_deadline: total_weeks <= max_weeks
CHECK_14: Количество automation-сессий * daily_practice_minutes реалистично для estimated_sessions каждого навыка.
```

#### 4. Проверка согласованности с профилем (Profile Alignment)
```
CHECK_15: Формат theory-единиц соответствует theory_format_preference.
CHECK_16: Тип обратной связи в practice-единицах соответствует feedback_type.
CHECK_17: Формат инструкций соответствует instruction_format.
CHECK_18: Если effective_level == "absolute_beginner": базовый уровень содержит пропедевтику.
CHECK_19: Контекст задач (ПП, ПМ) соответствует context_type.
```

#### 5. Проверка FSM-сценариев (Phase C readiness)
```
CHECK_20: Каждый lesson_blueprint содержит все 7 компонентов (ПП, ГП, КИ, ПМ, ПТ, СЗ, РФ).
CHECK_21: Каждый lesson_blueprint содержит правила переходов FSM.
CHECK_22: Правила покрывают все типы ответов ученика из таблицы состояний.
```

### Выходные данные

| Поле | Тип | Описание |
|---|---|---|
| `validation_passed` | boolean | Пройдена ли валидация |
| `check_results` | `CheckResult[]` | Результат каждой проверки |
| `critical_failures` | string[] | Критические ошибки (блокируют генерацию) |
| `warnings` | string[] | Некритические предупреждения |
| `fix_suggestions` | `FixSuggestion[]` | Предложения по исправлению |

### Действия при провале
```
ЕСЛИ critical_failures.length > 0:
    ДЛЯ КАЖДОЙ critical_failure:
        Определить: какой шаг (B1-B7) нужно перезапустить.
        Перезапустить шаг с исправленными входными данными.
    Повторить B8.
    
    Максимум итераций: 3.
    ЕСЛИ после 3 итераций всё ещё есть critical_failures:
        Вернуть трек с предупреждениями + рекомендация по ручной корректировке.
```

---

## ТИПЫ ДАННЫХ (TypeScript)

```typescript
// ============================================================
// ШАГ B1: Валидация и обогащение профиля
// ============================================================

type EffectiveLevel = 'absolute_beginner' | 'beginner' | 'intermediate_ready' | 'intermediate' | 'advanced';

interface ValidatedStudentProfile extends StudentProfile {
  effective_level: EffectiveLevel;
  enrichment_log: string[];
  validation_warnings: string[];
  estimated_weeks_range: [number, number];
}

// ============================================================
// ШАГ B2: Компетенции
// ============================================================

interface Competency {
  id: string;                           // "K1", "K2", ...
  statement: string;                    // "Способен [действие] в условиях [контекст]"
  level: 'understanding' | 'application' | 'analysis' | 'synthesis';  // Уровень по Блуму
  is_integral: boolean;                 // true для интегральной компетенции (из peak_task)
  related_task_ids: string[];           // Связанные задачи из target_tasks
  related_outcome_indices: number[];    // Индексы покрываемых desired_outcomes
}

interface CompetencySet {
  competencies: Competency[];
  integral_competency_id: string;
  competency_task_map: Record<string, string[]>;      // competency_id → task_ids
  competency_outcome_map: Record<string, number[]>;   // competency_id → outcome_indices
}

// ============================================================
// ШАГ B3: Матрица ЗУН
// ============================================================

interface KnowledgeItem {
  id: string;                           // "Z1", "Z2", ...
  competency_id: string;                // К какой компетенции относится
  description: string;                  // "Знать таблицу истинности для импликации..."
  source_type: 'confusing_concept' | 'barrier' | 'approach_gap' | 'subtask_requirement' | 'prerequisite';
  source_id: string;                    // ID источника (concept_id, barrier_id и т.д.)
  is_prerequisite: boolean;             // true если это пропедевтическое знание
}

interface SkillItem {
  id: string;                           // "U1", "U2", ...
  competency_id: string;
  description: string;                  // "Уметь строить таблицы истинности..."
  source_subtask_id: string;            // ID подзадачи-источника
  required_knowledge_ids: string[];     // Какие знания нужны
  pre_existing: boolean;                // true если студент уже владеет (already_known_subtasks)
  complexity_estimate: 'low' | 'medium' | 'high';
}

interface HabitItem {
  id: string;                           // "N1", "N2", ...
  competency_id: string;
  description: string;                  // "Навык быстрого определения истинности..."
  based_on_skill_id: string;            // На каком Умении построен
  mastery_criterion: string;            // "Решает без ошибок за < 30 сек"
  source_type: 'mastery_signal' | 'success_criterion' | 'derived';
}

interface DependencyEdge {
  from_id: string;                      // ID предпосылки
  to_id: string;                        // ID зависимого элемента
  type: 'knowledge_supports_skill' | 'skill_forms_habit' | 'knowledge_prerequisite';
}

interface KSAMatrix {
  knowledge_items: KnowledgeItem[];
  skill_items: SkillItem[];
  habit_items: HabitItem[];
  dependency_graph: DependencyEdge[];
}

// ============================================================
// ШАГ B4: Учебные единицы
// ============================================================

type UnitType = 'theory' | 'practice' | 'automation';

interface LearningUnitBase {
  id: string;                           // "LU-T-001", "LU-P-001", "LU-A-001"
  type: UnitType;
  title: string;
  description: string;
  estimated_minutes: number;
  prerequisites: string[];              // ID предпосылочных единиц
  ksa_item_ids: string[];               // Какие элементы ЗУН покрывает
  competency_id: string;                // К какой компетенции относится
  level: 'basic' | 'intermediate' | 'advanced' | 'integration';
}

interface TheoryUnit extends LearningUnitBase {
  type: 'theory';
  content_outline: string[];            // Ключевые пункты контента
  format: 'visual_schema' | 'example_first' | 'video' | 'discussion' | 'text_formulas';
  key_points: string[];                 // Главные тезисы
}

interface PracticeUnit extends LearningUnitBase {
  type: 'practice';
  task_template: string;                // Шаблон задания
  instruction_type: 'checklist' | 'video' | 'simulator' | 'reference_card';
  feedback_strategy: string;            // Стратегия обратной связи
  difficulty_variants: DifficultyVariant[];
  reference_solution: string;           // Эталонное решение
  related_knowledge_ids: string[];      // Связанные theory-единицы
}

interface DifficultyVariant {
  level: 'with_hints' | 'standard' | 'challenge';
  description: string;
  task: string;
}

interface AutomationUnit extends LearningUnitBase {
  type: 'automation';
  exercise_template: string;            // Шаблон упражнений
  repetition_count: number;             // Рекомендуемое число повторений за сессию
  mastery_threshold: string;            // Порог освоения
  estimated_sessions: number;           // Оценка количества сессий
  related_skill_ids: string[];          // На каких умениях построен
}

type LearningUnit = TheoryUnit | PracticeUnit | AutomationUnit;

interface LearningUnitCluster {
  id: string;
  competency_id: string;
  theory_units: TheoryUnit[];           // Поддерживающая информация (Компонент 2)
  practice_unit: PracticeUnit;          // Процедурная информация + учебная задача (Компоненты 1, 3)
  automation_units: AutomationUnit[];   // Частичная практика (Компонент 4)
}

// ============================================================
// ШАГ B5: Иерархия и уровни
// ============================================================

interface TrackLevel {
  id: string;
  name: 'basic' | 'intermediate' | 'advanced' | 'integration';
  display_name: string;                 // "Базовый", "Средний", "Продвинутый", "Интеграционный"
  description: string;                  // Что студент будет уметь после уровня
  units: LearningUnit[];
  clusters: LearningUnitCluster[];
  estimated_weeks: number;
  estimated_minutes: number;
}

// ============================================================
// ШАГ B6: Проблемные формулировки
// ============================================================

interface ProblemStatement {
  formulation: string;                  // Текст проблемы
  form: 'problem_statement' | 'problem_question' | 'authentic_task' | 'case_study';
  context_fit: string;                  // Как проблема связана с primary_context
}

interface Hypothesis {
  description: string;                  // Ожидаемая гипотеза ученика
  correctness: 'correct' | 'partial' | 'incorrect' | 'no_hypothesis';
  diagnostic_level: 'no_knowledge' | 'misconceptions' | 'partial' | 'solid_base';
  system_reaction: string;              // Следующее состояние FSM
  reaction_content: string;             // Что система говорит/делает
}

interface KIPortion {
  order: number;                        // 1, 2, 3...
  content: string;                      // Содержание порции теории
  format: string;                       // Формат подачи
  followed_by_task_id: string;          // ID задания ПМ, следующего за этой порцией
}

interface ApplicationTask {
  id: string;
  order: number;
  task_text: string;                    // Текст задания
  reference_answer: string;             // Эталонный ответ
  typical_errors: TypicalError[];       // Типичные ошибки
  feedback_on_success: string;
  feedback_on_partial: string;
  feedback_on_failure: string;
}

interface TypicalError {
  description: string;
  feedback: string;                     // Обратная связь при этой ошибке
  next_state: string;                   // Следующее состояние FSM
}

interface Contradiction {
  description: string;                  // Описание противоречия
  trigger_question: string;             // Вопрос, обнажающий противоречие
  related_barrier_id: string | null;    // Связь с барьером из профиля
  expected_reactions: {
    reaction: 'interest' | 'confusion' | 'ignore' | 'resolve' | 'partial_resolve';
    system_response: string;
    next_state: string;
  }[];
}

interface SynthesisTask {
  task_text: string;                    // Задание на синтез
  completeness_criteria: {
    full: string;                       // Критерии полного ответа
    partial: string;                    // Критерии неполного
    incorrect: string;                  // Критерии ошибочного
  };
}

interface ReflectionQuestion {
  question: string;
  depth_criteria: {
    deep: string;                       // Что считается глубокой рефлексией
    surface: string;                    // Что считается поверхностной
    absent: string;                     // Что считается отсутствием рефлексии
  };
}

interface Hint {
  target_state: string;                 // В каком состоянии FSM используется
  hint_text: string;                    // Текст подсказки
  trigger_condition: string;            // Когда применять (молчание, "не знаю", ошибка)
}

interface TransitionRule {
  from_state: string;                   // ПП, ГП, КИ, ПМ, ПТ, СЗ, РФ, ПЗ
  student_response_type: string;        // Тип ответа ученика
  to_state: string;                     // Следующее состояние
  action: string;                       // Что делает система при переходе
}

interface LessonBlueprint {
  id: string;
  cluster_id: string;                   // Связь с LearningUnitCluster
  level: 'basic' | 'intermediate' | 'advanced' | 'integration';
  topic_title: string;
  
  problem_statement: ProblemStatement;
  expected_hypotheses: Hypothesis[];
  key_information_portions: KIPortion[];
  application_tasks: ApplicationTask[];
  contradiction: Contradiction;
  synthesis_task: SynthesisTask;
  reflection_questions: ReflectionQuestion[];
  hints: Hint[];
  fsm_transition_rules: TransitionRule[];
}

// ============================================================
// ШАГ B7: Расписание
// ============================================================

interface TrackDay {
  day_of_week: string;
  main_units: LearningUnit[];           // Основные единицы (theory + practice)
  automation_units: AutomationUnit[];   // Микро-тренировки
  lesson_blueprint_ids: string[];       // Связанные сценарии уроков
  total_minutes: number;
}

interface Checkpoint {
  id: string;
  type: 'level_assessment' | 'mid_level_check' | 'final_assessment';
  week_number: number;
  level: string;
  description: string;
  criteria: string[];                   // Что проверяется
  pass_threshold: number;               // % для прохождения (0-100)
  on_fail_strategy: string;             // Что делать при непрохождении
  related_success_criteria_ids: string[];
}

interface FinalAssessment extends Checkpoint {
  type: 'final_assessment';
  format: string;                       // "пробный экзамен", "итоговый проект" и т.д.
  simulates: string;                    // Что имитирует (целевая деятельность)
}

interface Milestone {
  id: string;
  week_number: number;
  description: string;                  // "Освоен базовый уровень", "50% трека пройдено"
  metric: string;
  target_value: string;
}

interface SupportPlan {
  reminders: {
    enabled: boolean;
    schedule: string[];                 // Время напоминаний
  };
  mentor_checkpoints: {
    enabled: boolean;
    frequency: string;                  // "еженедельно", "после каждого уровня"
  } | null;
  progress_tracking: {
    enabled: boolean;
    milestones: Milestone[];
  };
}

interface TrackWeek {
  week_number: number;
  level: string;
  theme: string;                        // Тема недели
  weekly_goals: string[];               // Что студент будет уметь к концу недели
  days: TrackDay[];
  checkpoint: Checkpoint | null;
  total_minutes: number;
}

// ============================================================
// ШАГ B8: Валидация
// ============================================================

interface CheckResult {
  check_id: string;                     // "CHECK_1", "CHECK_2", ...
  description: string;
  passed: boolean;
  details: string;                      // Описание проблемы (если не пройден)
  severity: 'critical' | 'warning' | 'info';
}

interface FixSuggestion {
  check_id: string;
  target_step: string;                  // "B2", "B4", ...
  suggestion: string;
}

interface ValidationResult {
  validation_passed: boolean;
  check_results: CheckResult[];
  critical_failures: string[];
  warnings: string[];
  fix_suggestions: FixSuggestion[];
}

// ============================================================
// ИТОГОВАЯ СТРУКТУРА: PersonalizedTrack
// ============================================================

interface PersonalizedTrack {
  // Метаданные
  id: string;
  created_at: string;
  student_profile_id: string;
  
  // Из B1
  validated_profile: ValidatedStudentProfile;
  
  // Из B2
  competency_set: CompetencySet;
  
  // Из B3
  ksa_matrix: KSAMatrix;
  
  // Из B4
  learning_units: LearningUnit[];
  unit_clusters: LearningUnitCluster[];
  
  // Из B5
  levels: TrackLevel[];
  unit_sequence: LearningUnit[];
  
  // Из B6
  lesson_blueprints: LessonBlueprint[];
  
  // Из B7
  weeks: TrackWeek[];
  total_weeks: number;
  checkpoints: Checkpoint[];
  final_assessment: FinalAssessment;
  support_plan: SupportPlan;
  progress_milestones: Milestone[];
  
  // Из B8
  validation: ValidationResult;
}
```

---

## ГРАФ ДАННЫХ: ОТ ПРОФИЛЯ К ТРЕКУ

```
StudentProfile (Фаза A)
    │
    ├─ topic, subject_area ──────────────────────────────────► B6 (контекст ПП)
    ├─ experience_level + diagnostic_result ──► B1 → effective_level ─► B2, B4, B5, B6
    ├─ desired_outcomes ─────────────────────► B2 → competencies ──────► B3
    ├─ target_tasks, task_hierarchy ─────────► B2 → competencies ──────► B3
    ├─ peak_task_id, subtasks ──────────────► B2, B3 → KSA ───────────► B4
    ├─ confusing_concepts ──────────────────► B3 → knowledge_items ────► B4 → theory_units
    ├─ key_barriers ────────────────────────► B3, B6 → КИ, ПТ
    ├─ approach_gaps ────────────────────────► B3, B6 → ПТ
    ├─ mastery_signals ─────────────────────► B3 → habit_items ────────► B4 → automation_units
    ├─ success_criteria ────────────────────► B3, B5, B7 → checkpoints
    ├─ theory_format_preference ────────────► B4, B6 → формат theory + КИ
    ├─ instruction_format ──────────────────► B4 → формат practice
    ├─ feedback_type ───────────────────────► B4, B6 → обратная связь
    ├─ practice_format ─────────────────────► B4 → тип practice
    ├─ daily_practice_minutes ──────────────► B4, B7 → automation + расписание
    ├─ weekly_hours, schedule ──────────────► B5, B7 → бюджет времени + расписание
    ├─ has_deadline, deadline_date ──────────► B5, B7 → ограничение длительности
    ├─ already_known_subtasks ──────────────► B3, B5 → сокращение единиц
    ├─ learning_format ─────────────────────► B7 → support_plan
    ├─ support_tools, needs_reminder ───────► B7 → support_plan
    └─ role, primary_context, context_type ─► B4, B6 → контекст задач и ПП
```

---

## ПОРЯДОК ВЫЗОВА LLM И ОЦЕНКА СЛОЖНОСТИ

| Шаг | Количество LLM-вызовов | Оценка токенов на вызов | Примечание |
|---|---|---|---|
| B1 | 1 | 1000-2000 | Валидация + обогащение |
| B2 | 1-2 | 2000-4000 | Формулирование компетенций |
| B3 | 1 на компетенцию (2-7) | 2000-3000 каждый | Декомпозиция ЗУН |
| B4 | 1 на элемент ЗУН (10-30) | 1000-2000 каждый | Генерация учебных единиц |
| B5 | 1-2 | 2000-4000 | Иерархия и уровни |
| B6 | 1 на кластер (5-15) | 3000-5000 каждый | Самый тяжёлый шаг |
| B7 | 1-2 | 2000-4000 | Сборка расписания |
| B8 | 1 | 2000-3000 | Валидация |

**Оптимизация:** Шаги B3 и B4 можно объединить в один LLM-вызов на компетенцию: «Для компетенции X сгенерируй ЗУН и сразу учебные единицы». Это сократит количество вызовов в ~2 раза.

**Общая оценка:** 20-60 LLM-вызовов для полного трека. При батчинге — 8-15 вызовов.

---

## ПРИМЕР: СКВОЗНОЙ ПРОХОД (тема — мат. логика)

### Входные данные (из фазы A):
```
topic: "Элементы математической логики"
experience_level: "beginner"
diagnostic_result: "misconceptions"
desired_outcomes: ["решать задачи на таблицы истинности", "строить отрицания с кванторами"]
peak_task: "Построение отрицаний для высказываний с вложенными кванторами"
weekly_hours: 5
```

### B1 → effective_level = "beginner" (beginner × misconceptions)

### B2 → Компетенции:
```
К1: Способен применять аппарат булевой алгебры для анализа и преобразования логических выражений.
К2: Способен формализовывать высказывания с использованием кванторов и выполнять логические операции над ними.
К3: Способен анализировать истинность сложных логических утверждений на конкретных множествах.
```

### B3 → Матрица ЗУН (фрагмент для К1):
```
З1: Знать таблицы истинности для ¬, ∧, ∨, ⇒, ⇔
З2: Знать законы де Моргана и дистрибутивности
У1: Уметь строить таблицы истинности для составных высказываний
У2: Уметь доказывать тождества через таблицы
Н1: Навык быстрого определения истинности простых формул
Н2: Навык аккуратного заполнения таблиц истинности
```

### B4 → Учебные единицы (фрагмент):
```
LU-T-001: "Импликация и эквивалентность: почему таблицы выглядят именно так?" (theory, 20 мин)
LU-P-001: "Тренажёр: постройте таблицу истинности для (A ⇒ B) ∧ (¬B)" (practice, 30 мин)
LU-A-001: "Блиц: определи истинность за 15 секунд" (automation, 10 мин/сессия)
```

### B5 → Уровни:
```
Базовый (нед. 1-2): З1, У1, Н1, Н2
Средний (нед. 3): З2, У2
Продвинутый (нед. 4): З3, З4, У3, У4, Н3
Интеграционный (нед. 5): пробный экзамен
```

### B6 → ПП для базового уровня:
```
"Представьте, друг говорит: 'Если я получу премию, то куплю тебе кофе'.
В каких ситуациях он сдержит слово, а в каких — солжёт?"
```

### B7 → Расписание (неделя 1):
```
Пн: Theory "Импликация" (30 мин) + Automation "Блиц" (10 мин)
Вт: Automation "Блиц" (10 мин)
Ср: Practice "Тренажёр таблиц" (40 мин) + Automation "Блиц" (10 мин)
Чт: Automation "Блиц" (10 мин)
Пт: Practice "Проверка: импликация vs эквивалентность" (30 мин) + Automation "Блиц" (10 мин)
Сб: Theory "Законы логики" (60 мин) + Practice "Упрощение формул" (60 мин)
Вс: Theory повторение (30 мин) + Automation "Блиц" (10 мин) + Practice закрепление (20 мин)
Итого: ~5 часов
```

---

## ПРОВЕРКА СОГЛАСОВАННОСТИ С ФАЗОЙ A

| Поле из StudentProfile | Как используется в фазе B | Шаг(и) |
|---|---|---|
| `topic`, `subject_area` | Контекст генерации всего контента | B2, B3, B4, B6 |
| `experience_level` | Определение effective_level, режим генерации | B1 |
| `diagnostic_result` | Корректировка effective_level | B1 |
| `desired_outcomes` | Формулирование компетенций, проверка покрытия | B2, B8 |
| `target_tasks` | Основа компетенций и иерархии | B2, B5 |
| `task_hierarchy` | Порядок уровней | B5 |
| `peak_task_id` | Интегральная компетенция | B2 |
| `subtasks` | Генерация Умений | B3 |
| `confusing_concepts` | Генерация Знаний | B3, B8 |
| `key_barriers` | Знания + ПТ в уроках | B3, B6 |
| `approach_gaps` | Знания + ПТ в уроках | B3, B6 |
| `already_known_subtasks` | Сокращение единиц | B3, B5 |
| `theory_format_preference` | Формат theory-единиц и КИ | B4, B6 |
| `instruction_format` | Формат practice-единиц | B4 |
| `feedback_type` | Обратная связь в practice и ПМ | B4, B6 |
| `practice_format` | Тип practice-единиц | B4 |
| `daily_practice_minutes` | Объём automation, расписание | B4, B7 |
| `practice_windows` | Расписание микро-тренировок | B7 |
| `needs_reminder` | План поддержки | B7 |
| `mastery_signals` | Генерация Навыков | B3 |
| `support_tools` | План поддержки | B7 |
| `weekly_hours` | Бюджет времени, длительность трека | B5, B7 |
| `schedule` | Распределение по дням | B7 |
| `has_deadline`, `deadline_date` | Ограничение длительности | B1, B5, B7 |
| `success_criteria` | Навыки, точки контроля, финальная оценка | B3, B5, B7, B8 |
| `learning_format` | План поддержки | B7 |
| `role` | Контекст задач и примеров | B4, B6 |
| `primary_context`, `context_type` | Стиль задач и ПП | B4, B5, B6 |
| `novice_mode` | Влияет на effective_level → объём пропедевтики | B1, B2 |
