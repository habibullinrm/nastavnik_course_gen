# ФАЗА A: СБОР ДАННЫХ И ПРОФИЛИРОВАНИЕ (v2)

## Общие принципы фазы A

### Цель
Получить от пользователя структурированные ответы для построения профиля, на основе которого будет сгенерирован персонализированный учебный трек.

### Формат
Пошаговый диалог: система задаёт вопросы → пользователь отвечает → система извлекает данные.

### Проблема новичка
Многие вопросы предполагают, что пользователь уже ориентируется в предметной области (может назвать задачи, термины, подзадачи). **Полный новичок** этого не может. Поэтому каждый шаг содержит:
- **Основной сценарий** — пользователь способен ответить самостоятельно.
- **Сценарий новичка (fallback)** — пользователь не может ответить; система помогает: предлагает варианты, генерирует контент по теме, задаёт упрощённые вопросы.

### Степени значимости извлекаемых данных

Каждое извлекаемое поле помечено одной из трёх степеней:

| Значимость | Обозначение | Что означает | Последствие при отсутствии |
|---|---|---|---|
| **Критическая** | 🔴 `CRITICAL` | Без этих данных генерация трека **невозможна**. | Система обязана получить данные: переспрашивает, предлагает варианты, генерирует сама с подтверждением пользователя. |
| **Важная** | 🟡 `IMPORTANT` | Данные значительно **повышают качество** персонализации. | Система использует разумные значения по умолчанию, но предупреждает пользователя, что трек может быть менее точным. |
| **Дополнительная** | 🟢 `OPTIONAL` | Данные **улучшают** трек, но не критичны. | Система пропускает шаг или использует значения по умолчанию без предупреждения. |

### Определение темы обучения

Перед началом сбора данных система должна знать **тему обучения** — это нулевой шаг, без которого невозможен ни один последующий вопрос.

---

## БЛОК 0. Определение темы обучения

### Шаг 0.1 — Тема и предметная область

| Параметр | Значение |
|---|---|
| **Название** | `identify_topic` |
| **Что делает система** | Запрашивает у пользователя тему, которую он хочет изучить. Уточняет предметную область, если тема слишком широкая. Это **первый и обязательный** шаг — без темы невозможно задавать дальнейшие вопросы. |
| **Вопрос пользователю** | «Какую тему или навык вы хотите изучить? Опишите как можно конкретнее — например, не просто "математика", а "элементы математической логики" или "теория вероятностей".» |
| **Пример ответа (конкретный)** | «Элементы математической логики — таблицы истинности, логические операции, кванторы.» |
| **Пример ответа (размытый)** | «Хочу изучить программирование.» |
| **Fallback при размытом ответе** | Система задаёт уточняющий вопрос: «Программирование — это очень широкая область. Подскажите: 1) Какой язык или технологию вы имеете в виду? 2) Для чего вам это нужно (web-сайты, анализ данных, мобильные приложения, автоматизация)? 3) Есть ли конкретная задача, которую вы хотите уметь решать?» Система итерирует, пока тема не станет достаточно конкретной для генерации учебных задач. |

**Извлекаемые данные:**

| Поле | Тип | Значимость | Описание |
|---|---|---|---|
| `topic` | string | 🔴 `CRITICAL` | Название темы обучения |
| `subject_area` | string | 🔴 `CRITICAL` | Предметная область (математика, программирование, маркетинг и т.д.) |
| `topic_scope` | string | 🟡 `IMPORTANT` | Границы темы — что входит, что не входит |

**Зачем для генерации курса:**
- `topic` + `subject_area` → система использует эти данные для генерации учебных задач (шаг 2.1 fallback), формулировки проблем (фаза C), подбора примеров.
- `topic_scope` → ограничивает объём трека, позволяет не уходить в смежные области.

---

## БЛОК 1. Контекст и мотивация

### Шаг 1.1 — Идентификация роли и уровня

| Параметр | Значение |
|---|---|
| **Название** | `identify_role_and_level` |
| **Что делает система** | Запрашивает текущую роль, уровень опыта и наличие формального обучения. На основе ответа определяет: нужен ли «режим новичка» для дальнейших вопросов. |
| **Вопросы пользователю** | 1. «Кто вы? Расскажите о себе: чем занимаетесь, кем работаете или учитесь?» 2. «Какой у вас опыт в теме [topic]? Выберите: (а) Полный ноль — никогда не сталкивался; (б) Начинающий — что-то слышал/читал, но не практиковал; (в) С опытом — решал задачи, но есть пробелы; (г) Продвинутый — хочу углубить конкретный аспект.» 3. «Проходили ли вы какое-либо обучение по этой теме раньше? (курсы, вуз, самообучение)» |
| **Пример ответа** | «Я студент технического вуза, 2 курс. Начинающий — была вводная лекция, но многого не понял. Изучал в рамках курса "Дискретная математика", но чувствую, что отстаю.» |
| **Пример ответа (полный новичок)** | «Я менеджер. Полный ноль в логике, никогда не сталкивался. Никакого обучения не было.» |

**Извлекаемые данные:**

| Поле | Тип | Значимость | Описание | Зачем для генерации курса |
|---|---|---|---|---|
| `role` | string | 🟡 `IMPORTANT` | Текущая роль/профессия | Влияет на контекст примеров и задач (студенту — учебные, менеджеру — рабочие) |
| `experience_level` | enum [zero, beginner, intermediate, advanced] | 🔴 `CRITICAL` | Уровень опыта в теме | **Главный переключатель**: определяет, какие вопросы задавать дальше (режим новичка vs. стандартный). Влияет на начальный уровень сложности трека. |
| `formal_education` | string | 🟢 `OPTIONAL` | Предыдущее обучение | Помогает понять, какие базовые знания могут быть, даже если забыты |
| `identified_risks` | string[] | 🟡 `IMPORTANT` | Выявленные риски | Влияет на объём базового уровня и количество поддержки |

**Системное действие после шага 1.1:**
```
ЕСЛИ experience_level == "zero" ИЛИ "beginner":
    УСТАНОВИТЬ novice_mode = true
    → На шагах 2.1–3.3 активировать fallback-сценарии
ИНАЧЕ:
    УСТАНОВИТЬ novice_mode = false
    → Стандартные вопросы
```

---

### Шаг 1.2 — Выявление мотивации

| Параметр | Значение |
|---|---|
| **Название** | `identify_motivation` |
| **Что делает система** | Запрашивает мотивацию. Определяет тип целей (внешняя/внутренняя). |
| **Вопрос пользователю** | «Зачем вам это нужно? Что побудило вас начать учить [topic]? Например: сдать экзамен, решить рабочую задачу, сменить профессию, личный интерес, просто стало любопытно...» |
| **Пример ответа** | «Сдать экзамен на 4-5. Ещё хочу разобраться в основах для будущих предметов.» |
| **Пример ответа (новичок)** | «Просто интересно, хочу понять, что это такое. Может пригодится на работе.» |

**Извлекаемые данные:**

| Поле | Тип | Значимость | Описание | Зачем для генерации курса |
|---|---|---|---|---|
| `motivation_external` | string \| null | 🟡 `IMPORTANT` | Внешняя мотивация (экзамен, работа, дедлайн) | Определяет: есть ли жёсткий дедлайн и целевая деятельность. Если есть — трек строится «к экзамену/проекту». |
| `motivation_internal` | string \| null | 🟢 `OPTIONAL` | Внутренняя мотивация (интерес, развитие) | Влияет на стиль подачи: больше «почему» и «зачем» для внутренне мотивированных |
| `goal_type` | enum [applied, fundamental, mixed] | 🟡 `IMPORTANT` | Тип цели | `applied` → фокус на практике, `fundamental` → фокус на понимании, `mixed` → баланс |
| `has_deadline` | bool | 🟡 `IMPORTANT` | Есть ли жёсткий срок | Если true — система спрашивает дедлайн и сжимает трек |
| `deadline_date` | date \| null | 🟡 `IMPORTANT` (если has_deadline) | Дата дедлайна | Ограничивает длительность трека сверху |

---

### Шаг 1.3 — Образ результата

| Параметр | Значение |
|---|---|
| **Название** | `identify_desired_outcome` |
| **Что делает система** | Запрашивает, что пользователь хочет уметь делать после обучения. |
| **Вопрос пользователю** | «Представьте, что обучение прошло идеально. Что вы теперь умеете? Что изменилось? Опишите конкретные действия, которые вы сможете выполнять.» |
| **Пример ответа** | «Смогу самостоятельно решать задачи на таблицы истинности, строить отрицания для утверждений с кванторами. На экзамене буду уверенно выполнять задания.» |
| **Пример ответа (новичок, не может описать)** | «Не знаю точно... Хочу просто разбираться в теме.» |
| **Fallback для новичка** | Система предлагает варианты на основе `topic`: «Вот что обычно умеют люди, освоившие [topic]: (а) [типовой результат 1]; (б) [типовой результат 2]; (в) [типовой результат 3]. Что из этого вам ближе? Или опишите своё.» Варианты генерируются системой на основе знаний о предметной области. |

**Извлекаемые данные:**

| Поле | Тип | Значимость | Описание | Зачем для генерации курса |
|---|---|---|---|---|
| `desired_outcomes` | string[] | 🔴 `CRITICAL` | Целевые действия после обучения | Формируют «вершину мастерства» — интегральную цель трека. Без этого невозможно определить, куда вести студента. |
| `target_context` | string | 🟡 `IMPORTANT` | Контекст применения результатов | Определяет формат задач: экзаменационный, рабочий, бытовой |
| `outcome_source` | enum [user, system_suggested, mixed] | 🟢 `OPTIONAL` (служебное) | Откуда пришли outcomes | Если system_suggested — система помнит, что нужно валидировать результаты на более поздних этапах |

---

## БЛОК 2. Учебные задачи (Компонент 1 модели 4C/ID)

> **Ключевой блок.** Здесь формируется ядро будущего трека — иерархия задач, от простых к сложным.
> Для полных новичков этот блок работает принципиально иначе: система сама генерирует задачи и предлагает их на выбор.

### Шаг 2.1 — Сбор типичных задач

| Параметр | Значение |
|---|---|
| **Название** | `collect_target_tasks` |
| **Что делает система** | Запрашивает 2-3 конкретные задачи, которые пользователь хочет научиться выполнять. |

**Основной сценарий** (`novice_mode == false`):

| Параметр | Значение |
|---|---|
| **Вопрос пользователю** | «Опишите 2-3 самые типичные и важные задачи, которые вы хотите научиться выполнять. Максимально конкретно — не "изучить маркетинг", а "составить план запуска нового продукта для малого бизнеса".» |
| **Пример ответа** | «1) Строить таблицы истинности для сложных формул. 2) Доказывать логические законы через таблицы. 3) Строить отрицания для высказываний с кванторами.» |

**Сценарий новичка** (`novice_mode == true`):

| Параметр | Значение |
|---|---|
| **Что делает система** | Генерирует список из 5-7 типовых задач по теме `topic` с кратким пояснением каждой. Предлагает пользователю выбрать 2-3 наиболее интересные/нужные. |
| **Сообщение пользователю** | «Поскольку вы только начинаете изучать [topic], я подготовил список типичных задач, которые обычно решают в этой области. Выберите 2-3 задачи, которые вам кажутся наиболее важными или интересными: (1) [Задача 1] — краткое описание, что это и зачем. (2) [Задача 2] — ... (3) ... Можете также описать свою задачу, если она не в списке.» |
| **Пример сгенерированного списка (тема: мат. логика)** | «(1) Определять истинность логических высказываний — понимать, когда утверждение "Если A, то B" истинно или ложно. (2) Строить таблицы истинности — систематически проверять все варианты для сложных формул. (3) Упрощать логические выражения — приводить длинные формулы к более короткому виду. (4) Работать с кванторами "для всех" и "существует" — формализовывать утверждения про множества. (5) Строить отрицания сложных утверждений — "переворачивать" логические формулы. Что из этого вам нужнее всего?» |
| **Пример ответа новичка** | «Наверное, 1, 2 и 4. Хотя я не очень понимаю, что такое кванторы.» |
| **Дополнительное действие системы** | Если пользователь выбрал задачу, отметив, что не понимает её — это фиксируется как дополнительный барьер (`key_barriers`). |

**Извлекаемые данные:**

| Поле | Тип | Значимость | Описание | Зачем для генерации курса |
|---|---|---|---|---|
| `target_tasks` | Task[] | 🔴 `CRITICAL` | Массив целевых задач | Ядро трека: всё содержание строится вокруг этих задач. Каждая задача → учебная единица. |
| `tasks_source` | enum [user, system_generated, mixed] | 🟢 `OPTIONAL` (служебное) | Кто сформулировал задачи | Если system_generated — система берёт ответственность за полноту и может дополнить на этапе B |

---

### Шаг 2.2 — Ранжирование задач по сложности

| Параметр | Значение |
|---|---|
| **Название** | `rank_tasks` |
| **Что делает система** | Определяет иерархию задач: от самой простой до «вершины мастерства». |

**Основной сценарий** (`novice_mode == false`):

| Параметр | Значение |
|---|---|
| **Вопрос пользователю** | «Проранжируйте выбранные задачи: какая кажется самой простой для старта? Какая — самой сложной, ваша "вершина мастерства"?» |
| **Пример ответа** | «Простая — таблицы для простых формул. Сложная — отрицания с вложенными кванторами.» |

**Сценарий новичка** (`novice_mode == true`):

| Параметр | Значение |
|---|---|
| **Что делает система** | Система сама предлагает ранжирование на основе предметной логики и просит подтверждение. |
| **Сообщение пользователю** | «По сложности эти задачи обычно идут так: 1️⃣ [Самая простая] — с неё начнём; 2️⃣ [Средняя]; 3️⃣ [Самая сложная] — это будет ваша "вершина мастерства", к ней мы придём в конце. Согласны с таким порядком? Или вам кажется, что что-то стоит поменять?» |
| **Пример ответа новичка** | «Да, звучит логично.» или «Мне кажется, 2 и 3 надо поменять местами, потому что...» |

**Извлекаемые данные:**

| Поле | Тип | Значимость | Описание | Зачем для генерации курса |
|---|---|---|---|---|
| `task_hierarchy` | Task[] | 🔴 `CRITICAL` | Упорядоченный массив задач (от простой к сложной) | Определяет последовательность всего трека: базовый → средний → продвинутый уровень. |
| `easiest_task_id` | string | 🔴 `CRITICAL` | ID самой простой задачи | Точка старта — первые учебные единицы строятся вокруг неё |
| `peak_task_id` | string | 🔴 `CRITICAL` | ID «вершины мастерства» | Интегральная цель трека; формирует главную компетенцию |

---

### Шаг 2.3 — Декомпозиция сложной задачи на подзадачи

| Параметр | Значение |
|---|---|
| **Название** | `decompose_hardest_task` |
| **Что делает система** | Разбивает «вершину мастерства» на подзадачи — шаги, из которых она состоит. |

**Основной сценарий** (`novice_mode == false`):

| Параметр | Значение |
|---|---|
| **Вопрос пользователю** | «Попробуйте описать подзадачи, из которых состоит самая сложная задача. Из каких шагов она складывается?» |
| **Пример ответа** | «1. Определить структуру утверждения: где кванторы. 2. Знать правило: ¬∀ = ∃¬. 3. Последовательно спускать отрицание внутрь. 4. Упрощать выражение.» |

**Сценарий новичка** (`novice_mode == true`):

| Параметр | Значение |
|---|---|
| **Что делает система** | Система сама генерирует декомпозицию и представляет её пользователю для подтверждения. |
| **Сообщение пользователю** | «Чтобы научиться [peak_task], обычно нужно уметь делать вот эти вещи: 1. [Подзадача 1] — краткое объяснение. 2. [Подзадача 2] — краткое объяснение. 3. [Подзадача 3] — краткое объяснение. 4. [Подзадача 4] — краткое объяснение. Звучит логично? Может быть, что-то из этого вы уже умеете?» |
| **Пример ответа новичка** | «Понятно. Наверное, я ничего из этого пока не умею.» или «Пункт 1 вроде бы понимаю, остальное — нет.» |
| **Дополнительное действие** | Ответ «я уже умею X» помогает скорректировать стартовый уровень (обновить `experience_level` или сузить `key_barriers`). |

**Извлекаемые данные:**

| Поле | Тип | Значимость | Описание | Зачем для генерации курса |
|---|---|---|---|---|
| `subtasks` | Subtask[] | 🔴 `CRITICAL` | Подзадачи вершины мастерства | Каждая подзадача → будущее Умение (У) в матрице ЗУН → практическое задание в треке |
| `subtasks_source` | enum [user, system_generated, mixed] | 🟢 `OPTIONAL` (служебное) | Кто декомпозировал | Если system_generated — система может уточнить на этапе B |
| `already_known_subtasks` | string[] | 🟡 `IMPORTANT` | Подзадачи, которые пользователь уже умеет | Позволяет пропустить или сократить соответствующие учебные единицы |

---

### Шаг 2.4 — Контекст применения

| Параметр | Значение |
|---|---|
| **Название** | `identify_application_context` |
| **Что делает система** | Определяет, где пользователь будет применять навык. Влияет на стиль задач и примеров в треке. |
| **Вопрос пользователю** | «Где вы будете применять то, чему научитесь? Например: на экзамене, в рабочих проектах, в личных задачах, просто для общего развития?» |
| **Пример ответа** | «На экзамене, но также хочу понимать условия в программировании.» |
| **Пример ответа (новичок)** | «Пока не знаю. Наверное, для общего развития.» |
| **Fallback** | Если пользователь не может определить контекст, система ставит значение по умолчанию `general` и помечает, что контекст может быть уточнён позже. |

**Извлекаемые данные:**

| Поле | Тип | Значимость | Описание | Зачем для генерации курса |
|---|---|---|---|---|
| `primary_context` | string | 🟡 `IMPORTANT` | Основной контекст применения | Определяет стиль задач: академические, рабочие, бытовые |
| `secondary_context` | string \| null | 🟢 `OPTIONAL` | Дополнительный контекст | Обогащает примеры, добавляет прикладные задачи |
| `context_type` | enum [academic, professional, personal, general] | 🟡 `IMPORTANT` | Тип контекста | Влияет на формулировки ПП (практических проблем) в фазе C |

---

## БЛОК 3. Диагностика понимания и предпочтений (Компонент 2 — Поддерживающая информация)

> Этот блок сильнее всего зависит от уровня пользователя.
> Для `experience_level == "zero"` шаги 3.1–3.2 существенно упрощаются.

### Шаг 3.1 — Диагностика текущего подхода

| Параметр | Значение |
|---|---|
| **Название** | `diagnose_current_approach` |
| **Что делает система** | Проверяет, как пользователь сейчас решает задачи по теме. Выявляет ментальную модель и пробелы. |

**Основной сценарий** (`novice_mode == false`):

| Параметр | Значение |
|---|---|
| **Вопрос пользователю** | «Опишите, как вы сейчас подходите к решению задачи [простейшая задача из шага 2.1]. Какие шаги предпринимаете?» |
| **Примечание** | Система берёт `easiest_task` из шага 2.2 и формулирует конкретный пример задачи. |
| **Пример ответа** | «Пытаюсь вспомнить таблицу для импликации, путаю, когда она ложная. Делаю столбец для ¬B, потом пытаюсь соединить. Часто ошибаюсь.» |

**Сценарий новичка** (`novice_mode == true`):

| Параметр | Значение |
|---|---|
| **Что делает система** | Вместо открытого вопроса даёт мини-диагностическое задание — простейшую задачу по теме — и просит попробовать решить или хотя бы описать свои мысли. |
| **Сообщение пользователю** | «Давайте проверим, с чего вы начинаете. Вот простая задача: [формулировка простейшей задачи по теме]. Попробуйте решить или просто опишите, что приходит в голову. Можно ответить "понятия не имею" — это тоже ценная информация.» |
| **Пример задачи (мат. логика)** | «Утверждение: "Если идёт дождь, то улица мокрая". Дождя нет, улица мокрая. Утверждение истинно или ложно? Ваши мысли?» |
| **Пример ответа новичка** | «Мне кажется, ложно, потому что дождя нет, а улица мокрая — значит, причина другая...» или «Понятия не имею.» |
| **Что извлекает система** | Даже неправильный ответ показывает ментальную модель. Ответ «понятия не имею» → фиксируем, что стартовый уровень — абсолютный ноль, нужно начинать с самых базовых понятий. |

**Извлекаемые данные:**

| Поле | Тип | Значимость | Описание | Зачем для генерации курса |
|---|---|---|---|---|
| `current_approach` | string \| null | 🟡 `IMPORTANT` | Описание текущей стратегии решения | Показывает, какие ментальные модели есть и какие ошибочны — это формирует «противоречия» (ПТ) в фазе C |
| `approach_gaps` | string[] | 🟡 `IMPORTANT` | Выявленные ошибки и пробелы | Напрямую формируют блок «Знания» (З): каждый пробел → тема для теории |
| `diagnostic_result` | enum [no_knowledge, misconceptions, partial, solid_base] | 🔴 `CRITICAL` | Итоговый уровень по диагностике | Корректирует `experience_level` из шага 1.1. Определяет стартовую точку трека. |

**Примечание:** `diagnostic_result` является **критическим**, потому что самооценка пользователя (шаг 1.1) часто не совпадает с реальным уровнем. Человек может считать себя «начинающим», а диагностика покажет `no_knowledge` или наоборот `solid_base`.

---

### Шаг 3.2 — Выявление трудностей

| Параметр | Значение |
|---|---|
| **Название** | `identify_difficulties` |
| **Что делает система** | Выявляет конкретные концептуальные барьеры. |

**Основной сценарий** (`novice_mode == false`):

| Параметр | Значение |
|---|---|
| **Вопрос пользователю** | «С какими самыми большими трудностями вы сталкиваетесь, когда пытаетесь решить [сложная задача из шага 2.2]?» |
| **Пример ответа** | «Не понимаю, как "переворачивать" кванторы. Запутался в разнице между ∀ и ∃.» |

**Сценарий новичка** (`novice_mode == true`):

| Параметр | Значение |
|---|---|
| **Что делает система** | Вместо открытого вопроса о трудностях (новичок не знает, чего не знает) система предлагает список типичных трудностей и спрашивает, что резонирует. |
| **Сообщение пользователю** | «Люди, начинающие изучать [topic], часто сталкиваются с такими трудностями: (а) [Типичная трудность 1] — пояснение; (б) [Типичная трудность 2] — пояснение; (в) [Типичная трудность 3] — пояснение; (г) У меня нет трудностей — я просто ничего не знаю по теме. Что из этого вам ближе?» |
| **Пример списка (мат. логика)** | «(а) Сложно понять, почему "Если ложь, то что угодно" — это истина; (б) Путаю операции И/ИЛИ/НЕ между собой; (в) Не понимаю, что такое кванторы "для всех" и "существует"; (г) Я просто ничего не знаю — начинаю с нуля.» |
| **Пример ответа новичка** | «Наверное, (г) — я вообще ничего не знаю.» или «(а) и (в) — звучит пугающе, но именно это хочу понять.» |

**Извлекаемые данные:**

| Поле | Тип | Значимость | Описание | Зачем для генерации курса |
|---|---|---|---|---|
| `key_barriers` | Barrier[] | 🟡 `IMPORTANT` | Концептуальные барьеры | Каждый барьер → тема для теории + точка для «противоречия» (ПТ) в фазе C. При `diagnostic_result == no_knowledge` система генерирует барьеры автоматически. |
| `barriers_source` | enum [user, system_suggested, auto_generated] | 🟢 `OPTIONAL` (служебное) | Источник барьеров | Если auto_generated — барьеры уточняются в ходе обучения (фаза C) |

**Правило для `no_knowledge`:** Если `diagnostic_result == no_knowledge`, система не задаёт этот вопрос, а автоматически генерирует список базовых барьеров для темы:
```
ЕСЛИ diagnostic_result == "no_knowledge":
    key_barriers = generateDefaultBarriers(topic, subject_area)
    barriers_source = "auto_generated"
    ПРОПУСТИТЬ вопрос пользователю
```

---

### Шаг 3.3 — Выявление путаницы в терминах

| Параметр | Значение |
|---|---|
| **Название** | `identify_confusing_concepts` |
| **Что делает система** | Выявляет конкретные термины и концепции, вызывающие путаницу. |

**Основной сценарий** (`novice_mode == false`):

| Параметр | Значение |
|---|---|
| **Вопрос пользователю** | «Какие ключевые термины или концепции в [topic] вызывают у вас наибольшую путаницу или кажутся "размытыми"?» |
| **Пример ответа** | «Импликация — почему истинна при ложной посылке? Эквивалентность — чем отличается? Кванторы — как читать?» |

**Сценарий новичка** (`novice_mode == true` ИЛИ `diagnostic_result == no_knowledge`):

| Параметр | Значение |
|---|---|
| **Что делает система** | Система пропускает этот вопрос и автоматически формирует список ключевых терминов темы, которые нужно изучить. |
| **Логика** | Новичок не может назвать «путающие» термины, если не знает ни одного. Система генерирует полный список базовых терминов темы как обязательный контент. |
| **Сообщение пользователю (необязательное)** | «Для изучения [topic] вам понадобится освоить следующие ключевые понятия: [список]. Мы разберём их постепенно, от простых к сложным.» |

**Извлекаемые данные:**

| Поле | Тип | Значимость | Описание | Зачем для генерации курса |
|---|---|---|---|---|
| `confusing_concepts` | Concept[] | 🔴 `CRITICAL` | Термины, требующие объяснения | Напрямую формируют блок «Знания» (З) в матрице ЗУН. Каждый термин → обязательная тема в теоретическом блоке трека. |
| `concepts_source` | enum [user, auto_generated] | 🟢 `OPTIONAL` (служебное) | Источник списка | Если auto_generated — это полный базовый словарь темы, а не точечные пробелы |

**Примечание:** для новичков `confusing_concepts` по сути превращается в «словарь темы» — полный список терминов, которые нужно освоить. Для опытных пользователей — это точечные пробелы.

---

### Шаг 3.4 — Предпочтения по формату теории

| Параметр | Значение |
|---|---|
| **Название** | `identify_theory_preferences` |
| **Что делает система** | Определяет, в каком формате пользователю легче всего усваивать новую информацию. |
| **Вопрос пользователю** | «Как вам легче всего разбираться в новых сложных вещах? Выберите то, что вам ближе: (а) Через наглядные схемы и таблицы; (б) Через разбор конкретных примеров — "покажи, как это работает"; (в) Через видео с объяснением; (г) Через разговор/обсуждение — задать вопросы и получить ответы; (д) Через чтение текста с формулами. А также: расскажите про самый полезный учебный материал, который вы когда-либо встречали. Что в нём было хорошего?» |
| **Пример ответа** | «(а) и (б). Помогли конспекты-шпаргалки, где формулы были с примерами на одной схеме.» |
| **Пример ответа (новичок)** | «Мне нравятся видео и схемы. Примеры — вообще отлично.» |

**Извлекаемые данные:**

| Поле | Тип | Значимость | Описание | Зачем для генерации курса |
|---|---|---|---|---|
| `theory_format_preference` | enum [visual_schemas, examples_first, video, discussion, text_formulas, mixed] | 🟡 `IMPORTANT` | Предпочитаемый формат теории | Определяет формат подачи КИ (ключевой информации) в фазе C и формат учебных единиц типа `theory` в треке |
| `theory_format_details` | string | 🟢 `OPTIONAL` | Описание идеального формата | Калибрует стиль подачи (краткие шпаргалки vs. развёрнутые тексты) |
| `best_material_reference` | string | 🟢 `OPTIONAL` | Что было полезно раньше | Помогает подобрать аналогичный стиль |

**Значения по умолчанию:** если пользователь не ответил → `theory_format_preference = "examples_first"` (универсально работающий формат).

---

## БЛОК 4. Предпочтения по практике (Компоненты 3-4 модели 4C/ID)

> Этот блок одинаково работает для всех уровней — вопросы касаются предпочтений, а не знаний по теме.

### Шаг 4.1 — Формат инструкций

| Параметр | Значение |
|---|---|
| **Название** | `identify_instruction_preferences` |
| **Что делает система** | Определяет предпочтительный формат инструкций к практическим заданиям. |
| **Вопрос пользователю** | «Когда вы выполняете практическое задание, в каком виде вам удобнее получать инструкции? (а) Пошаговый чек-лист: "Шаг 1 → Шаг 2 → Шаг 3"; (б) Видео, где кто-то решает аналогичную задачу (2-3 мин); (в) Интерактивный тренажёр — делаешь шаг, система проверяет, переходишь дальше; (г) Справочная карточка: все правила на одной странице.» |
| **Пример ответа** | «(а) и (б). Чек-лист с примером + короткое видео.» |

**Извлекаемые данные:**

| Поле | Тип | Значимость | Описание | Зачем для генерации курса |
|---|---|---|---|---|
| `instruction_format` | string[] | 🟡 `IMPORTANT` | Предпочитаемый формат инструкций | Определяет формат процедурной информации (Компонент 3 модели 4C/ID) и формат учебных единиц типа `practice` |

**Значения по умолчанию:** если не ответил → `["checklist"]`.

---

### Шаг 4.2 — Формат обратной связи и отработки

| Параметр | Значение |
|---|---|
| **Название** | `identify_feedback_preferences` |
| **Что делает система** | Определяет, как пользователь хочет получать обратную связь при выполнении заданий. |
| **Вопросы пользователю** | «Когда вы делаете задание и ошибаетесь, что вам помогает больше? (а) Сразу узнать, что ошибка, и увидеть правильный ответ; (б) Подсказка, где ошибка, но правильный ответ искать самому; (в) Полный разбор ошибки с объяснением "почему так"; (г) Сначала попробовать самому, а потом сравнить с эталоном.» |
| **Пример ответа** | «(в) и (г). Хочу понимать, почему ошибся, и сравнить с образцом.» |

**Извлекаемые данные:**

| Поле | Тип | Значимость | Описание | Зачем для генерации курса |
|---|---|---|---|---|
| `feedback_type` | string[] | 🟡 `IMPORTANT` | Предпочтения по обратной связи | Определяет поведение системы при ответе ученика в фазе C: как именно реагировать на ошибки (ПЗ, КИ) |
| `practice_format` | string[] | 🟡 `IMPORTANT` | Формат тренировки | Определяет тип учебных единиц `practice`: тренажёр, сравнение с эталоном, шаблон |

**Значения по умолчанию:** `feedback_type = ["error_with_explanation"]`, `practice_format = ["compare_with_standard"]`.

---

### Шаг 4.3 — Режим автоматизации (микро-тренировки)

| Параметр | Значение |
|---|---|
| **Название** | `identify_automation_mode` |
| **Что делает система** | Определяет готовность к ежедневным коротким тренировкам для доведения навыков до автоматизма. |
| **Вопросы пользователю** | «Для закрепления навыков полезно делать короткие ежедневные упражнения (как зарядка для мозга). 1) Сколько минут в день вы готовы на это тратить? (5 / 10 / 15 / 20 / не готов) 2) Когда удобнее: утром, в обед, вечером? На каком устройстве?» |
| **Пример ответа** | «10 минут утром и 10 вечером. На телефоне. Но нужно напоминание, иначе забуду.» |

**Извлекаемые данные:**

| Поле | Тип | Значимость | Описание | Зачем для генерации курса |
|---|---|---|---|---|
| `daily_practice_minutes` | int | 🟡 `IMPORTANT` | Минут в день на микро-тренировки | Определяет объём учебных единиц типа `automation` и количество упражнений в ежедневных блоках |
| `practice_windows` | PracticeWindow[] | 🟢 `OPTIONAL` | Временные окна | Для персонализации расписания в треке |
| `needs_reminder` | bool | 🟢 `OPTIONAL` | Нужно ли напоминание | Для планирования механизмов поддержки |

**Значения по умолчанию:** `daily_practice_minutes = 10`, `practice_windows = []`, `needs_reminder = true`.

---

### Шаг 4.4 — Критерии освоения навыка и поддержка

| Параметр | Значение |
|---|---|
| **Название** | `identify_mastery_signals` |
| **Что делает система** | Определяет субъективные маркеры успеха и необходимую поддержку. |
| **Вопросы пользователю** | «1) Как вы поймёте, что уже освоили навык? По каким признакам? (например, "перестану ошибаться", "смогу решить без подглядывания", "объясню другому") 2) Что поможет вам не бросить тренировки? (напоминания, трекер прогресса, поддержка ментора, ничего не нужно)» |
| **Пример ответа** | «Перестану путать кванторы. Смогу решать без шпаргалок. Нужно напоминание в календарь.» |
| **Пример ответа (новичок)** | «Пока не знаю, как это будет выглядеть... Наверное, смогу решить хотя бы простую задачу.» |
| **Fallback** | Если не может ответить: система предлагает стандартные маркеры: «Обычно люди считают навык освоенным, когда: (а) Решают типовые задачи без подсказок; (б) Укладываются в отведённое время; (в) Могут объяснить тему другому человеку. Подходит?» |

**Извлекаемые данные:**

| Поле | Тип | Значимость | Описание | Зачем для генерации курса |
|---|---|---|---|---|
| `mastery_signals` | string[] | 🟢 `OPTIONAL` | Субъективные маркеры освоения | Формируют блок «Навыки» (Н) в матрице ЗУН: что должно быть доведено до автоматизма |
| `support_tools` | string[] | 🟢 `OPTIONAL` | Необходимая поддержка | Встраивается в трек: напоминания, шаблоны, трекер прогресса |

**Значения по умолчанию:** `mastery_signals = ["решает типовые задачи без подсказок"]`, `support_tools = ["progress_tracker"]`.

---

## БЛОК 5. Организационные ограничения

### Шаг 5.1 — Временной бюджет

| Параметр | Значение |
|---|---|
| **Название** | `identify_time_budget` |
| **Что делает система** | Определяет еженедельный бюджет времени и распределение по дням. |
| **Вопрос пользователю** | «Сколько часов в неделю вы можете посвятить обучению? Как обычно распределяете: понемногу каждый день или большой блок на выходных?» |
| **Пример ответа** | «5 часов: 2 в субботу, 1 в воскресенье, по 30-40 минут в будни вечером.» |

**Извлекаемые данные:**

| Поле | Тип | Значимость | Описание | Зачем для генерации курса |
|---|---|---|---|---|
| `weekly_hours` | float | 🔴 `CRITICAL` | Часов в неделю | Определяет длительность трека: total_weeks = total_content_hours / weekly_hours. Без этого невозможно построить расписание. |
| `schedule` | Schedule[] | 🟡 `IMPORTANT` | Распределение по дням | Определяет, какие дни отводятся на теорию (длинные блоки), какие на практику (короткие). |

**Значения по умолчанию:** `schedule` = равномерное распределение `weekly_hours` по 7 дням.

---

### Шаг 5.2 — Формат обучения

| Параметр | Значение |
|---|---|
| **Название** | `identify_learning_format` |
| **Что делает система** | Определяет, как пользователь хочет учиться. |
| **Вопрос пользователю** | «Как вам комфортнее учиться? (а) Полностью самостоятельно по материалам; (б) Самостоятельно, но с возможностью задать вопрос наставнику; (в) В группе с другими учащимися; (г) Смешанный формат.» |
| **Пример ответа** | «(б). Самостоятельно, но хочу иметь возможность спросить в чате.» |

**Извлекаемые данные:**

| Поле | Тип | Значимость | Описание | Зачем для генерации курса |
|---|---|---|---|---|
| `learning_format` | enum [self_paced, mentored, group, blended] | 🟢 `OPTIONAL` | Формат обучения | Определяет наличие/отсутствие элементов сопровождения в треке |
| `support_channel` | string | 🟢 `OPTIONAL` | Канал поддержки | Для планирования коммуникации в рамках трека |

**Значения по умолчанию:** `learning_format = "self_paced"`.

---

### Шаг 5.3 — Критерии успеха

| Параметр | Значение |
|---|---|
| **Название** | `identify_success_criteria` |
| **Что делает система** | Определяет измеримые критерии успеха — KPI трека. |
| **Вопрос пользователю** | «Как мы поймём, что обучение прошло успешно? Что будет результатом? (например: сдать экзамен на X баллов, выполнить проект, решить Y% задач без ошибок)» |
| **Пример ответа** | «Сдам экзамен на 4-5. На пробнике решу ≥80% задач. Смогу объяснить другому.» |
| **Пример ответа (новичок)** | «Не знаю... Просто хочу разобраться.» |
| **Fallback** | Если пользователь не может сформулировать: система предлагает стандартные критерии: «Давайте установим такой ориентир: после обучения вы сможете самостоятельно решить [количество] типовых задач по [topic] с точностью [процент]. Подходит? Или предложите свой вариант.» |

**Извлекаемые данные:**

| Поле | Тип | Значимость | Описание | Зачем для генерации курса |
|---|---|---|---|---|
| `success_criteria` | Criterion[] | 🔴 `CRITICAL` | Измеримые критерии успеха | Определяют итоговую оценку трека: содержание финальной недели (пробный экзамен, итоговый проект), порог прохождения. Без критериев трек не имеет точки завершения. |

---

## СВОДКА: ВСЕ ИЗВЛЕКАЕМЫЕ ПОЛЯ С ПРИОРИТЕТАМИ

### 🔴 CRITICAL — без них трек НЕ генерируется (7 полей)

| # | Поле | Шаг | Откуда | Fallback для новичка |
|---|---|---|---|---|
| 1 | `topic` | 0.1 | Пользователь | Итеративное уточнение |
| 2 | `subject_area` | 0.1 | Пользователь | Система определяет по topic |
| 3 | `experience_level` | 1.1 | Пользователь | Варианты с пояснением (zero/beginner/intermediate/advanced) |
| 4 | `desired_outcomes` | 1.3 | Пользователь / система | Система предлагает типовые результаты на выбор |
| 5 | `target_tasks` + `task_hierarchy` + `peak_task` | 2.1, 2.2 | Пользователь / система | Система генерирует список задач по теме, пользователь выбирает |
| 6 | `subtasks` | 2.3 | Пользователь / система | Система генерирует декомпозицию, пользователь подтверждает |
| 7 | `confusing_concepts` | 3.3 | Пользователь / система | Система генерирует базовый словарь темы |
| 8 | `diagnostic_result` | 3.1 | Система (на основе ответа) | Мини-задание или авто-определение |
| 9 | `weekly_hours` | 5.1 | Пользователь | Переспрашиваем обязательно |
| 10 | `success_criteria` | 5.3 | Пользователь / система | Система предлагает стандартные критерии |

### 🟡 IMPORTANT — значительно повышают качество (12 полей)

| # | Поле | Шаг | Значение по умолчанию |
|---|---|---|---|
| 1 | `role` | 1.1 | "не указана" |
| 2 | `identified_risks` | 1.1 | [] |
| 3 | `motivation_external` | 1.2 | null |
| 4 | `goal_type` | 1.2 | "mixed" |
| 5 | `has_deadline` / `deadline_date` | 1.2 | false / null |
| 6 | `target_context` | 1.3 | "общее развитие" |
| 7 | `primary_context` / `context_type` | 2.4 | "general" |
| 8 | `current_approach` / `approach_gaps` | 3.1 | null / [] |
| 9 | `key_barriers` | 3.2 | auto_generated |
| 10 | `theory_format_preference` | 3.4 | "examples_first" |
| 11 | `instruction_format` | 4.1 | ["checklist"] |
| 12 | `feedback_type` / `practice_format` | 4.2 | ["error_with_explanation"] / ["compare_with_standard"] |
| 13 | `daily_practice_minutes` | 4.3 | 10 |
| 14 | `schedule` | 5.1 | равномерное распределение |
| 15 | `already_known_subtasks` | 2.3 | [] |

### 🟢 OPTIONAL — улучшают, но не критичны (11 полей)

| # | Поле | Шаг | Значение по умолчанию |
|---|---|---|---|
| 1 | `topic_scope` | 0.1 | определяется системой |
| 2 | `formal_education` | 1.1 | "" |
| 3 | `motivation_internal` | 1.2 | null |
| 4 | `secondary_context` | 2.4 | null |
| 5 | `theory_format_details` | 3.4 | "" |
| 6 | `best_material_reference` | 3.4 | "" |
| 7 | `practice_windows` | 4.3 | [] |
| 8 | `needs_reminder` | 4.3 | true |
| 9 | `mastery_signals` | 4.4 | ["решает без подсказок"] |
| 10 | `support_tools` | 4.4 | ["progress_tracker"] |
| 11 | `learning_format` / `support_channel` | 5.2 | "self_paced" / "" |
| 12 | Все поля `*_source` | служебные | определяется системой |

---

## ЛОГИКА ВЕТВЛЕНИЯ: ДЕРЕВО РЕШЕНИЙ ДЛЯ НОВИЧКА

```
Шаг 0.1: topic → ОБЯЗАТЕЛЬНО получить
    │
Шаг 1.1: experience_level
    │
    ├── "zero" или "beginner" ──────────────────► novice_mode = true
    │                                               │
    │   Шаг 2.1: система ГЕНЕРИРУЕТ задачи ◄───────┘
    │   Шаг 2.2: система ПРЕДЛАГАЕТ ранжирование
    │   Шаг 2.3: система ГЕНЕРИРУЕТ декомпозицию
    │   Шаг 3.1: система даёт МИНИ-ЗАДАНИЕ
    │   Шаг 3.2: система ПРЕДЛАГАЕТ типичные трудности
    │            ИЛИ ПРОПУСКАЕТ (если no_knowledge)
    │   Шаг 3.3: система ГЕНЕРИРУЕТ словарь темы
    │
    ├── "intermediate" ─────────────────────────► novice_mode = false
    │                                               │
    │   Шаг 2.1: пользователь ОПИСЫВАЕТ задачи ◄───┘
    │   Шаг 2.2: пользователь РАНЖИРУЕТ
    │   Шаг 2.3: пользователь ДЕКОМПОЗИРУЕТ
    │   Шаг 3.1: пользователь ОПИСЫВАЕТ подход
    │   Шаг 3.2: пользователь НАЗЫВАЕТ трудности
    │   Шаг 3.3: пользователь НАЗЫВАЕТ путаницу
    │
    └── "advanced" ─────────────────────────────► novice_mode = false
                                                    │
        Шаги 2.x, 3.x: стандартный сценарий ◄──────┘
        (пользователь хорошо формулирует)

    Шаги 3.4, 4.x, 5.x: ОДИНАКОВО ДЛЯ ВСЕХ УРОВНЕЙ
    (вопросы о предпочтениях, не о знаниях)
```

---

## ПОРЯДОК ШАГОВ И ВОЗМОЖНОСТЬ ОБЪЕДИНЕНИЯ

Некоторые шаги можно объединить в один вопрос, чтобы не перегружать пользователя:

| Группа | Шаги | Можно объединить? | Рекомендация |
|---|---|---|---|
| Знакомство | 0.1 + 1.1 | ✅ Да | Один экран: «Что хотите изучить? Расскажите о себе.» |
| Мотивация | 1.2 + 1.3 | ✅ Да | «Зачем вам это и что хотите уметь?» |
| Задачи | 2.1 + 2.2 | ✅ Да | «Какие задачи + какая самая простая/сложная?» |
| Декомпозиция + контекст | 2.3 + 2.4 | ⚠️ Условно | Лучше отдельно, т.к. 2.3 сложный |
| Диагностика | 3.1 + 3.2 + 3.3 | ⚠️ Условно | Для новичка: мини-задание (3.1) отдельно, 3.2+3.3 объединяются |
| Формат теории + практики | 3.4 + 4.1 + 4.2 | ✅ Да | «Как вам удобнее учиться?» |
| Автоматизация + поддержка | 4.3 + 4.4 | ✅ Да | «Ежедневные тренировки + что поможет не бросить?» |
| Оргвопросы | 5.1 + 5.2 + 5.3 | ✅ Да | «Время, формат, критерии — всё на одном экране» |

**Минимальное количество шагов-экранов при объединении:** 6-8 (вместо 16).

---

## ТИПЫ ДАННЫХ (TypeScript)

```typescript
// === Шаг 0.1: Тема ===
interface TopicInfo {
  topic: string;                    // 🔴 CRITICAL
  subject_area: string;             // 🔴 CRITICAL
  topic_scope: string;              // 🟡 IMPORTANT
}

// === Блок 1: Контекст и мотивация ===
interface ContextAndMotivation {
  role: string;                     // 🟡 IMPORTANT
  experience_level: 'zero' | 'beginner' | 'intermediate' | 'advanced';  // 🔴 CRITICAL
  formal_education: string;         // 🟢 OPTIONAL
  identified_risks: string[];       // 🟡 IMPORTANT

  motivation_external: string | null;   // 🟡 IMPORTANT
  motivation_internal: string | null;   // 🟢 OPTIONAL
  goal_type: 'applied' | 'fundamental' | 'mixed';  // 🟡 IMPORTANT
  has_deadline: boolean;            // 🟡 IMPORTANT
  deadline_date: string | null;     // 🟡 IMPORTANT (если has_deadline)

  desired_outcomes: string[];       // 🔴 CRITICAL
  target_context: string;           // 🟡 IMPORTANT
  outcome_source: 'user' | 'system_suggested' | 'mixed';  // 🟢 OPTIONAL (служебное)
}

// === Блок 2: Учебные задачи ===
interface Task {
  id: string;
  description: string;
  complexity_rank: number;
}

interface Subtask {
  id: string;
  description: string;
  parent_task_id: string;
  required_knowledge: string[];
  required_skills: string[];
}

interface LearningTasks {
  target_tasks: Task[];             // 🔴 CRITICAL
  tasks_source: 'user' | 'system_generated' | 'mixed';  // 🟢 OPTIONAL (служебное)

  task_hierarchy: Task[];           // 🔴 CRITICAL
  easiest_task_id: string;          // 🔴 CRITICAL
  peak_task_id: string;             // 🔴 CRITICAL

  subtasks: Subtask[];              // 🔴 CRITICAL
  subtasks_source: 'user' | 'system_generated' | 'mixed';  // 🟢 OPTIONAL (служебное)
  already_known_subtasks: string[]; // 🟡 IMPORTANT

  primary_context: string;          // 🟡 IMPORTANT
  secondary_context: string | null; // 🟢 OPTIONAL
  context_type: 'academic' | 'professional' | 'personal' | 'general';  // 🟡 IMPORTANT
}

// === Блок 3: Диагностика ===
interface Barrier {
  id: string;
  description: string;
  related_task_id: string;
  barrier_type: 'conceptual' | 'procedural' | 'motivational';
}

interface Concept {
  id: string;
  term: string;
  confusion_description: string;
}

interface Diagnostics {
  current_approach: string | null;  // 🟡 IMPORTANT
  approach_gaps: string[];          // 🟡 IMPORTANT
  diagnostic_result: 'no_knowledge' | 'misconceptions' | 'partial' | 'solid_base';  // 🔴 CRITICAL

  key_barriers: Barrier[];          // 🟡 IMPORTANT
  barriers_source: 'user' | 'system_suggested' | 'auto_generated';  // 🟢 OPTIONAL (служебное)

  confusing_concepts: Concept[];    // 🔴 CRITICAL
  concepts_source: 'user' | 'auto_generated';  // 🟢 OPTIONAL (служебное)

  theory_format_preference: 'visual_schemas' | 'examples_first' | 'video' | 'discussion' | 'text_formulas' | 'mixed';  // 🟡 IMPORTANT
  theory_format_details: string;    // 🟢 OPTIONAL
  best_material_reference: string;  // 🟢 OPTIONAL
}

// === Блок 4: Предпочтения по практике ===
interface PracticeWindow {
  time_of_day: string;
  duration_minutes: number;
  device: string;
}

interface PracticePreferences {
  instruction_format: string[];     // 🟡 IMPORTANT
  feedback_type: string[];          // 🟡 IMPORTANT
  practice_format: string[];        // 🟡 IMPORTANT

  daily_practice_minutes: number;   // 🟡 IMPORTANT
  practice_windows: PracticeWindow[];  // 🟢 OPTIONAL
  needs_reminder: boolean;          // 🟢 OPTIONAL

  mastery_signals: string[];        // 🟢 OPTIONAL
  support_tools: string[];          // 🟢 OPTIONAL
}

// === Блок 5: Организационные ограничения ===
interface Schedule {
  day_of_week: string;
  available_minutes: number;
}

interface Criterion {
  id: string;
  description: string;
  measurable: boolean;
  metric: string;
}

interface OrganizationalConstraints {
  weekly_hours: number;             // 🔴 CRITICAL
  schedule: Schedule[];             // 🟡 IMPORTANT

  learning_format: 'self_paced' | 'mentored' | 'group' | 'blended';  // 🟢 OPTIONAL
  support_channel: string;          // 🟢 OPTIONAL

  success_criteria: Criterion[];    // 🔴 CRITICAL
}

// === Полный профиль студента ===
interface StudentProfile extends
  TopicInfo,
  ContextAndMotivation,
  LearningTasks,
  Diagnostics,
  PracticePreferences,
  OrganizationalConstraints {
  
  // Служебное поле
  novice_mode: boolean;  // true если experience_level in ["zero", "beginner"]
}

// === Конфигурация шага сбора данных ===
interface CollectionStep {
  id: string;                       // "0.1", "1.1", ... "5.3"
  name: string;                     // "identify_topic", "identify_role_and_level", ...
  block: number;                    // 0, 1, 2, 3, 4, 5
  
  question_standard: string;        // Вопрос для стандартного режима
  question_novice: string | null;   // Вопрос для режима новичка (null = тот же)
  
  fallback_strategy: 'iterate' | 'suggest_options' | 'generate_and_confirm' | 'mini_task' | 'skip_and_default' | 'none';
  fallback_message: string | null;  // Сообщение при fallback
  
  extracted_fields: {
    field_name: string;
    type: string;
    significance: 'CRITICAL' | 'IMPORTANT' | 'OPTIONAL';
    default_value: any;
  }[];

  skip_condition: string | null;    // Условие пропуска шага, например: "diagnostic_result == 'no_knowledge'"
  depends_on: string[];             // ID шагов, от которых зависит (нужны их данные)
}
```

---

## ГРАФ ЗАВИСИМОСТЕЙ МЕЖДУ ШАГАМИ

```
0.1 (topic) ──────────────────────┐
                                  │
1.1 (role/level) ─────┐           │  всё зависит от topic
                      │           │
1.2 (motivation) ─────┤           │
                      ├── 1.3 (outcomes) ──── 2.1 (tasks)
1.1.experience_level  │                          │
       │              │                          ├── 2.2 (ranking)
       │              │                          │      │
       ▼              │                          │      ▼
  novice_mode ────────┼──────────────────────────┼── 2.3 (decompose)
       │              │                          │      │
       │              │                          │      ▼
       │              │                          └── 2.4 (context)
       │              │
       ├──────────────┼── 3.1 (diagnose) ← 2.2.easiest_task
       │              │      │
       │              │      ▼
       │              │   3.1.diagnostic_result
       │              │      │
       ├──────────────┼── 3.2 (difficulties) ← 2.2.peak_task
       │              │
       ├──────────────┼── 3.3 (concepts) ← topic
       │              │
       │              └── 3.4 (theory prefs) ── независимый
       │
       │              4.1 (instruction prefs) ── независимый
       │              4.2 (feedback prefs) ── независимый
       │              4.3 (automation) ── независимый
       │              4.4 (mastery signals) ── независимый
       │
       │              5.1 (time budget) ── независимый
       │              5.2 (learning format) ── независимый
       │              5.3 (success criteria) ← 1.2, 1.3
```
