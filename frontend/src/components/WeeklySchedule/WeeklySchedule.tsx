'use client';
export default function WeeklySchedule({ trackData }: { trackData: Record<string, unknown> }) {
  return <div className="p-4 bg-gray-50 rounded"><p className="text-gray-500">WeeklySchedule — в разработке</p></div>;
}
